---
project: TrSetup
stack: .NET 10 / Blazor (TrSetupUI RCL on TrBlazeUI) / MAUI Blazor Hybrid desktop (Windows + Mac Catalyst) / no DB / no RAG
last_updated: 2026-07-21
current_phase: Verified (phase-mac) — 3 defects fixed; Release-build defect retracted as unreal
last_verified_build: PASS
last_verified_date: 2026-07-21
---

# TrSetup — Status

## Where I am
**2026-07-21 — `*verify phase-mac` (first run against the real Catalyst head), then `*fix-issues` ×2.** The verify run drove the native head over Appium 3.5.2 + mac2@4.0.4 (`⚠ STATIC-ONLY` lifted); both gates PASS. It found 3 defects, **all fixed and re-verified**: the invalid Lucide icon (NFR-003 → back to `Verified`, now guarded), the engine/probe timeout race (FN-016 → 96), and native testability (FN-030 → 94, gated to Debug per owner decision). Unit 172 → **176**, Playwright 18 → **19**, 0 FAIL.

⚠ **A defect this document asserted twice — "`dotnet build -c Release` ships an empty `wwwroot`" — is RETRACTED. It was never real.** Release Catalyst builds two per-RID *inner* bundles and lipo-merges them into one **universal** bundle at `bin/Release/net10.0-maccatalyst/TrSetup.app`, **with no RID sub-folder**; that bundle is complete. The `maccatalyst-arm64/`+`x64/` folders are intermediates whose empty `wwwroot` is by design (`DisableResizetizer` / `_IsMultiRidBuild`). Debug builds a single RID, so *there* the RID folder is the deliverable — an asymmetry that made this look like a Release regression. Three investigations went to the wrong artifact because **our own BuildAndRun-Guide documented the wrong path**; the guide is fixed and the trap written down. Two consequences: **Release-head Appium drivability is now PROVEN** (universal bundle driven live — 209 nodes in the WebView subtree, real board text), and the Debug-only automation gate is verified **non-vacuously** for the first time (previously it was "confirmed" against a bundle containing nothing at all, which proved nothing). FN-016 and FN-030 stay `Implemented` on owner-bound halves only (a reboot; a Windows host). Per-REQ detail in the checklist Remarks.

**2026-07-21 — `*fix-issues FN-016 FN-028 FN-030 FN-036 FN-039` (flow-master, on the Mac VM).** None of the five was a code defect on entry; each was recorded as blocked on an environment. Three of those environments were opened and **two prior findings in this document were overturned**.

- **FN-016 — Appium stack was genuinely broken, not merely absent.** Root cause chain: `mac.node` pinned Node **v22.11.0**, which satisfies none of appium@3's engines (`^20.19.0 || ^22.12.0 || >=24.0.0`), so `npm i -g appium` silently resolved **2.0.1**; `APPIUM_HOME` was unset so the detect read a cwd-relative manifest while the fixer wrote elsewhere (two divergent driver sets existed: `mac2@4.0.4` in the repo, `mac2@2.2.2` in `~/.appium`, `xcuitest` in neither). Fixed: Node → **v22.23.1** with a `MinimumNodeVersion` gate, `APPIUM_HOME` pinned to `~/.trsetup/tools/appium`, driver guard rewritten (the old `grep -q` matched Appium's own "may be incompatible" WARN text and could never repair a stuck version), and the LaunchAgent plist now pins both `APPIUM_HOME` and `PATH`. Now live: **Appium 3.5.2 + xcuitest@11.17.7 + mac2@4.0.4**, mutually compatible, `/status` answering.
- **FN-028 — the `localhost` assumption was the bug.** The profile hardcoded `https://localhost:5101/` while its own roles list allows a Mac app-runner talking to a separate Windows device-host. App Manager actually runs on **192.168.1.14:5101**. Endpoint is now overridable per machine (`urlSettingKey` → `TrSetupSettings.Endpoints`, surfaced in Settings, resolved per detect so a save is picked up without restart). `appstudio.appmanager-api` **goes green against 192.168.1.14**; the Catalyst gate correctly narrows to the one genuinely-red prerequisite. Self-signed TLS is an explicit per-endpoint opt-in — validation is never disabled by default, and a profile's built-in default URL is always fully validated.
- **FN-030 — ⚠ REFUTED: agents CAN drive the MAUI Catalyst head.** With a working mac2 driver, a real WebDriver session attached and exposed a 104 KB tree containing `XCUIElementTypeWebView identifier="TrSetupBlazorWebView"` with **101 real Blazor DOM nodes inside it** — the exact strings previously recorded as absent (`Environment board`, `Fix all`, `Node.js`) are present, and a `click` on "Toggle Sidebar" changed the rendered UI. The old "XCUITest cannot expose Blazor Hybrid WebView content (4 approaches, all blocked)" finding was a misattribution: the app under test was rendering a blank 404 page, so there was no content in the DOM to see. `runtimeVerification.appium.maccatalyst` is now registered in `.tfcore/core-config.yaml` with the working recipe (launch via `open -a` FIRST, then a system-scoped session with **no** `appium:bundleId` capability — passing it makes Appium kill+relaunch and re-trigger the blank-content path). Drivability is established on the **Debug** head; the Release-published head attached only flakily, so Release drivability is **not** established.
- **FN-036 / FN-039 — untouched, still Windows-blocked.** The Windows host at 192.168.1.14 is reachable but exposes only SMB/445 (auth required) and RDP/3389 — **no remote-exec channel** (no SSH/22, no WinRM/5985-5986), so the Windows publish still cannot be driven from here. See Known blockers.

Verified by me directly, not taken from sub-agent reports: **unit 172/172**, **Playwright 17/17**, solution builds with the Catalyst head. Signing posture re-confirmed independently: `Signature=adhoc`, `TeamIdentifier=not set`, `spctl -a -t exec` → **rejected**, bundle universal (`x86_64 arm64`). New `docs/TrSetup-DeploymentGuide.md` (+ HTML) documents production deployment end-to-end. **Nothing was promoted to `Verified` this session — no verify-phase run was executed; all rows stay at `Implemented`.**


**2026-07-20 (evening) — `*verify phase-mac` executed on the Mac; nothing failed.** Playwright **15/15**, unit **149/149**, render §4a + visual §4b gates PASS at 1280×800 and 390×844 (screenshots inspected). Three rows promoted on real runtime evidence: **FN-008 → Verified** (the deferred live Mac run happened — all 7 F-MACCHK detects return true verdicts; the three Fails were each independently confirmed as *true* negatives, not false ones), **FN-022 → Verified** (clears `Needs re-verify` — the Xcode `-version` fix is live, `appstudio.xcode` now detects Xcode 26.6), **FN-027** re-confirmed. Also proven at binary level: `codesign -d --entitlements` on the built `TrSetup.app` reports `app-sandbox = false`, and `artifacts/TrSetup-1.0-macOS.dmg` mounts with the correct layout (TrSetup.app 156 MB + `/Applications` symlink). **The MAUI Catalyst head itself is ⚠ STATIC-ONLY** — build/entitlement-verified only; all render+visual evidence came from the `TrSetup.Web` smoke host running the same `TrSetupUI` RCL. FN-016/FN-028/FN-030 stay `Implemented`: each has one acceptance half that is owner-bound (a reboot, two owner-supplied prerequisites, a Windows host), not a defect.

**2026-07-20 — the Mac Catalyst head was App-Sandboxed, which is why no fixer could ever install anything.** Sandbox writes were redirected into `~/Library/Containers/com.techierathore.trsetup/Data/` (the MAUI head had its own private settings.json, and an owner Fix click had parked `dotnet-install.sh` inside the container); entitlement now `false`, rebuilt `.app` confirmed un-sandboxed. See FN-030 in the checklist.

TrSetup ships as **ONE MAUI Blazor Hybrid desktop app**: `src/TrSetup`; `TrSetup.Web` remains the test-only headless smoke host (:5999). **2026-07-20 — first run on a clean Mac (full Xcode 26.6, no .NET).** Toolchain installed user-local (.NET SDK 10.0.302 in `~/.dotnet` + full `maui` workload); the **Mac Catalyst `.app` builds** (156 MB, arm64+x64) and now packages to a **49 MB `.dmg`**. Six defects closed: **FN-030** App Sandbox (the reason no fixer could ever install anything), **FN-028** working dir (cwd → validated `AppRepoPaths`; refuses rather than building in the wrong place), **FN-016** ×3 (mac.node / mac.dotnet-maui / mac.appium-drivers post-fix PATH blindness, plus the missing .NET PATH block), and **FN-022** Xcode false negative (`xcodebuild --version` → exit 64; Apple tools take `-version`). Distribution is now specified (BRD-57..61 / FN-036..041) with a GitHub Actions pipeline that needs no Apple account. Unit **149/149**, Playwright **15/15** (was 9 failing). Nothing is marked `Verified` from this session: no verify-phase run was executed.

## Next command to run
```
/TechieFlow:agents:verifier *verify phase-mac
```
(No agent-fixable defect is currently open — the last one was retracted as unreal. A re-verify would grade FN-030 against the newly-proven Release artifact and re-confirm the three fixes. Everything else outstanding is **owner-bound**: the Mac reboot (FN-016's last acceptance half), a Windows remote-exec channel (FN-036/039), `APPMANAGER_API_KEY` (FN-028), and the Apple Developer account (FN-038). Also worth doing before the next verify run: add acceptance criteria to the six P7 rows, and amend FN-030's AutomationId clause, which today's evidence shows is unachievable as written.)

**Owner actions that unblock the rest — none of these are agent-doable:**
1. **Enable a remote-exec channel on the Windows host (192.168.1.14)** — install/start OpenSSH Server (`Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0; Start-Service sshd; Set-Service -Name sshd -StartupType Automatic`) or enable WinRM. Only SMB/445 and RDP/3389 are open today, neither of which permits running a build. This is the single blocker on FN-036 + FN-039.
2. **Export `APPMANAGER_API_KEY`** in the environment TrSetup reads — closes the last red prerequisite on the FN-028 Catalyst green path. TrSetup will never store this (ADR-008).
3. **Reboot the Mac** and confirm the Appium LaunchAgent comes back — FN-016's one remaining acceptance half. Note the LaunchAgent was deliberately *not* installed into `~/Library/LaunchAgents` this session (plist lint-tested to a temp path only), so install it first.

Then push a branch + add the `GH_PACKAGES_PAT` secret and run the **Build artifacts** workflow (FN-041) to produce the first CI `.dmg` + Windows zip.
Owner vision recorded 2026-07-20 as BRD-57..61: download one file per OS from the repo, run it on a fresh machine, and TrSetup installs everything. Today's build cannot do that — BRD-44 scoped it to "personal LAN use, ad-hoc signed", and BRD-45 (self-contained distribution publishes) was withdrawn 2026-07-09. Mac already bundles its runtime; Windows does not, which is the bootstrap paradox to close first.
Then owner-run UAT: walk docs/TrSetup-UsageGuide.md §"UAT plan" (FN-014/015/016/025/026, FN-008 §6.2, FN-028 green-path, FN-030 Windows visual).

## Open requirements
- [x] ~~**`dotnet build -c Release` ships a Catalyst `.app` with an EMPTY `wwwroot`**~~ (REQ-FN-030): **RETRACTED 2026-07-21 — never a real defect.** Release builds a *universal* bundle at `bin/Release/net10.0-maccatalyst/TrSetup.app` (no RID sub-folder) and it is complete; the `maccatalyst-arm64/`+`x64/` folders are intermediates whose empty wwwroot is by design (`DisableResizetizer` / `_IsMultiRidBuild`). We were inspecting the wrong artifact, because our own BuildAndRun-Guide documented the wrong path. Guide fixed and the trap written down.
- [ ] **Owner action — run the Windows packaging on a Windows host** (REQ-FN-039): `.\build\package-windows.ps1`, then launch the zip's exe on a CLEAN Windows image with no .NET Desktop Runtime and no Windows App SDK. The csproj self-contained switches are property-verified on this Mac but the artifact itself cannot be produced here (the TFM guard excludes the Windows TFM on macOS). That clean-machine launch is the only thing that closes BRD-57/BRD-60.
- [ ] **Owner decision — Apple Developer account** (REQ-FN-038, ~$99/yr): needed for Developer ID signing + notarization. Without it every Mac user must do the UsageGuide §1b trust step (documented and verified working, but not "download and run").
- [ ] **Owner action — add repository secret `GH_PACKAGES_PAT`** (read:packages for the techierathore feed) before the first CI run, or restore fails on TrBlazeUI 1.0.7 (FN-041).
- [ ] **Owner UAT — prove a real install from the MAUI head** now that the App Sandbox is off (FN-030): click Fix on a red row and confirm it lands on the real system, not a container. Still owner-manual **only because a Fix installs software** — no agent should click it uninvited. ~~Agents cannot drive the MAUI head~~ **(struck 2026-07-21 — refuted; a mac2 session reads and clicks Blazor DOM inside the WebView. See "Where I am".)**
- [x] **Bug — app launched under XCUITest shows "There is no content at"** (FN-030 follow-up): **fully root-caused 2026-07-21 — and the earlier root cause was itself wrong.** Not a launch-context fragility, and NOT a build defect either. The app being launched was a per-RID *intermediate* bundle (`maccatalyst-arm64/TrSetup.app`), whose `wwwroot` is empty by design; the real universal Release bundle sits one level up at `bin/Release/net10.0-maccatalyst/TrSetup.app` and renders correctly. Our own BuildAndRun-Guide documented the wrong path, which is what sent three separate investigations to the wrong artifact. Guide fixed; trap documented in the guide and in the FN-030 checklist row.
- [x] ~~**Release-head Appium drivability unproven**~~ (FN-030): **CLOSED 2026-07-21.** The universal Release `.app` was launched and driven over Appium mac2 — 412 nodes, **209 in the `TrSetupBlazorWebView` subtree**, live board text, zero "There is no content at". The old "application under test is not running" symptom was the same wrong-artifact mistake, not a Release fragility.
- [ ] **Owner UAT — Windows CI artifact** (FN-039/041): run the workflow, then launch the produced zip's exe on a clean Windows image with no .NET Desktop Runtime and no Windows App SDK.
- [ ] **Re-check PATH-sensitive rows on the real MAUI head**, not the web host: `dotnet TrSetup.Web.dll` is muxer-hosted and resolves tools absent from PATH, which the self-contained MAUI apphost cannot (proved 2026-07-20). Blocked behind the Accessibility grant above.
- [ ] Settings UI has no editor for the new `AppRepoPaths` (FN-028) — configurable only by hand-editing `~/.trsetup/settings.json`
- [ ] Changing Roles/SelectedApp does not rebuild the check catalog — it is a startup-time singleton snapshot, so the board silently serves stale rows until restart
- [ ] UAT (Implemented, external-host actions — owner runs): FN-014/015/016/025/026 destructive live installs (FN-016 also needs the reboot check — "LaunchAgent survives reboot" is its one unverified acceptance half) · FN-028 Catalyst fixer green-path (needs the two owner-supplied prereqs green) · FN-030 Windows visual board render. *(FN-008's §6.2 walk-through dropped 2026-07-20 — the row is now `Verified` on live evidence.)*

- [ ] **P7 distribution rows have no acceptance criteria** (REQ-FN-036/037/038/039/040/041): these six rows exist in the Requirements Status table but have **no Details anchor and no *Acceptance:* line**, so the verifier has nothing to grade them against and cannot promote them past `Implemented` no matter what it observes (FN-037's `.dmg` was mounted and confirmed good this run, and still could not be marked `Verified`). Add acceptance criteria for BRD-57..61 before the next verify run.

Counts: **35 Verified · 12 Implemented (external UAT / defect) · 6 N/A · 0 FAIL · 0 Needs re-verify · 1 PARTIAL · 1 Blocked (FN-038, owner-parked)** (of 55 rows incl. the FN-036..041 distribution + CI set).

## Known blockers
- **Windows host has no remote-exec channel (blocks FN-036 + FN-039).** 192.168.1.14 is up and reachable, but a port sweep found only **445 (SMB, auth required)** and **3389 (RDP)** — no SSH/22, no WinRM/5985-5986. SMB is file-transfer only and RDP is interactive-only, so `build\package-windows.ps1` cannot be driven from this Mac. Compounding it, the Windows publish cannot be produced on macOS at all (the csproj `IsOSPlatform('OSX')` guard replaces `TargetFrameworks` to avoid NETSDK1100; forcing it propagates a global property into `TrSetupUI`/`TrSetup.Core` → NETSDK1005). **The Windows artifact has still never been built or launched.** Owner must enable OpenSSH/WinRM, or run the script and hand back the artifact.
- AppStudio Catalyst green-path: **`appstudio.appmanager-api` is now GREEN** (2026-07-21) via the new per-machine endpoint override pointed at `https://192.168.1.14:5101/health` with the self-signed opt-in. One prerequisite remains: `appstudio.appmanager-secret` (`APPMANAGER_API_KEY` — TrSetup never stores secrets, ADR-008). `appstudio.xcode` and `appstudio.maui-workload` detect green.
- **Neither artifact is signed for distribution.** macOS is **ad-hoc only** (`CodesignKey=-`, `TeamIdentifier=not set`) and `spctl -a -t exec` returns **rejected** — the Gatekeeper block is current and real, not theoretical. Windows is entirely unsigned (no Authenticode cert, no `signtool` anywhere in the csproj, `package-windows.ps1`, or the CI workflow). Tracked as FN-038, owner-parked pending an Apple Developer account.
- `TrBlazeUI.Primitives.Select.SelectContent.DisposeAsync()` throws unhandled `JSDisconnectedException` on every circuit teardown (logs `fail:` per disconnect) — library-side, logged to the TrBlazeUI feedback file.
- Private feed: `TrBlazeUI` 1.0.7 needs a PAT-bearing `nuget.config` at the repo root (gitignored) on every new machine — restore fails against nuget.org alone.
- `AngleSharp` 0.17.1 carries a known moderate-severity advisory (GHSA-pgww-w46g-26qg).
- (Installer per-version SHA-256 pinning remains a deferred follow-up, not a blocker.)

## Verification log
| Date | Phase | Result | Status table |
|------|-------|--------|--------------|
| 2026-07-21 | fix-issues FN-030 — Release-build defect investigation (flow-master, Mac) | PASS — **no defect existed; finding RETRACTED**. `dotnet build -c Release` emits a *universal* Catalyst bundle at `bin/Release/net10.0-maccatalyst/TrSetup.app` (no RID sub-folder) with a complete `wwwroot`; the per-RID folders are intermediates, empty by design (`DisableResizetizer`/`_IsMultiRidBuild`). `@(StaticWebAsset)` 91 / `@(MauiAsset)` 89 identical across outer+inner builds — the pipeline was never at fault. Real defect was a wrong path in `TrSetup-BuildAndRun-Guide.md` §3 Option B (now fixed + trap documented). Closes two open items: **Release-head drivability PROVEN** (209 WebView nodes, live board text) and the Debug-only automation gate verified **non-vacuously**. My own gate target was exonerated by experiment first. unit 176/176, Playwright 19/19 | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-21 | fix-issues NFR-003 + FN-030 + FN-016 (flow-master, Mac) then re-verify | PASS — unit 172→**176**, Playwright 18→**19**, both verified by the orchestrator directly. NFR-003 → `Verified` (Lucide `sliders` is alias-only, never resolved; now `sliders-horizontal` + a raw-DOM `Icon not found:` guard across 5 routes, falsification-tested). FN-016 → 96 (engine budget now derived as probe+3s; the `mac.appium-launchagent` row finally shows the stale IP). FN-030 → 92 (native ids 0→48 via `data-testid`→`aria-placeholder`→XCUITest `value`; **owner-decided gate: Debug only**, Release bundle verified free of automation assets). Negative finding recorded: no HTML attribute can populate XCUITest `identifier` for WebView content. Confirmed on a clean build: `dotnet build -c Release` yields an EMPTY Catalyst `wwwroot` — ⚠ **this last claim was RETRACTED later the same day (see the row above): it was the wrong artifact, not a defect** | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-21 | verify-phase `phase-mac` (verifier, ON the Mac, **first run driving the real Catalyst head**, ledger docs/.last-verify.json) | PASS — 0 FAIL. Playwright 18/18, unit 172/172; §4a + §4b gates PASS on the native head over Appium 3.5.2 + mac2@4.0.4 (`⚠ STATIC-ONLY` lifted). FN-008 re-confirmed, FN-016 → 95, FN-030 → 90. Found 3 defects: invalid Lucide `sliders` icon (NFR-003 → `Needs re-verify`), no AutomationIds in the WebView (FN-030), engine/probe timeout race (FN-016) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-21 | fix-issues FN-016/028/030/036/039 (flow-master, Mac VM, 4 sub-agents) | PASS — **unit 172/172, Playwright 17/17** (both re-run by the orchestrator, not taken on report), solution + Catalyst head build. FN-016: Appium stack repaired (Node v22.11.0 → v22.23.1 unblocked appium@3; `APPIUM_HOME` pinned; now 3.5.2 + xcuitest@11.17.7 + mac2@4.0.4). FN-028: endpoint override added → `appstudio.appmanager-api` GREEN against 192.168.1.14; gate narrows to the one real red prereq. FN-030: **prior "agents cannot drive the MAUI head" finding REFUTED** — mac2 session read 101 Blazor DOM nodes inside the WebView and clicked one; core-config appium block registered; native-head screenshots captured. FN-036/039: untouched — Windows host has no remote-exec channel. Added `docs/TrSetup-DeploymentGuide.md` (+HTML). **No row promoted past `Implemented`; no verify-phase run executed, so nothing marked `Verified`.** | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-20 | verify-phase `phase-mac` (verifier, ON the Mac, ledger docs/.last-verify.json) | PASS — 0 FAIL. Playwright 15/15, unit 149/149, render+visual gates PASS (1280×800 + 390×844, screenshots inspected). FN-008 → Verified (deferred live Mac run executed; 7/7 detects true — global appium genuinely absent, IP drift real), FN-022 → Verified (Xcode false-negative fix confirmed live: Xcode 26.6 detected), FN-027 re-confirmed, UI-001..006 + NFR-003 re-confirmed. Binary-level: app-sandbox=false in the signed .app; .dmg mounts correctly. FN-016/028/030 stay Implemented (owner-bound acceptance halves). FOUND: P7 rows FN-036..041 have no acceptance criteria; cosmetic right-aligned Pass-row evidence on desktop (not a gate failure) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-20 | Distribution + CI + MAUI automation assessment (flow-master) | PASS — unit 149/149, Playwright 15/15; .dmg packaged; CI workflow added; MAUI head proven un-driveable by agents | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-20 | fix-issues FN-028 + FN-016 (flow-master, clean Mac) | PASS — build green, unit 141/141, Playwright 15/15; Catalyst .app built | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-05 | Day-1 (greenfield) | N/A — docs only, no build | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-06 | P1-UI + P2-fixers (build-phase) | PASS — build green (rung #4, 0W/0E), 99 tests, 5 UI pages smoked (render+visual) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-07 | P3 profiles + app fixers (build-phase) | PASS — build green, 140 tests, profiles E2E render-truth | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-07 | P4 distribution + MAUI head (build-phase) | PASS — build green, FN-031/NFR-006 verified inline, FN-030 Implemented | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-07 | P5 stretch — pre-flight gate (build-phase) | PASS — FN-033 verified inline; all REQs built | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-07 | verify all (flow-master inline, 2 waves) | 36/44 Verified, 0 FAIL; 8 rows documented UAT | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-07 | Handoff (flow-master) | READY FOR UAT — docs finalized + HTMLs re-rendered | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-09 | Scope change — MAUI-only (owner decision) | N/A — docs pass only: CLI+Web heads withdrawn, FN-034/NFR-007/UI-006 added | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-09 | P6 MAUI-only build-phase (flow-master, 3 parallel clusters) | PASS — build green rung #4; 126 Core.Tests; 7/7 UI specs. (Verdicts later found self-attested — see 2026-07-10 audit) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-09 | Handoff (flow-master) | READY FOR UAT — UsageGuide finalized; BRD rolled up; HTMLs re-rendered | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-09 | Mac copy-to-binaries path (flow-master) | BUILT + documented — `TrSetup.Web` self-contained osx-arm64 publish; BuildAndRun §3 Options A/B | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-10 | Verdict audit + naming standard (framework session) | FN-031/FN-034/NFR-007/UI-006 demoted to `Needs re-verify` (self-attested); REQ-FN-035 added | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-10 | P6 build-phase FN-035 rename + executed verify-phase (chained inline) | PASS — rename done; build rung #4 0W/0E; exe booted; 126 unit; 7/7 verify specs; ledger written. FN-031/034/NFR-007/UI-006/FN-035 → Verified; UI-001..005 re-confirmed | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-11 | fix-issues: unstyled Web board (owner screenshots issuessc/, ON the Mac) | PASS — root cause: content root defaulted to launch cwd, so a publish started elsewhere 404'd every static asset (raw HTML; the §3 A′ runbook itself triggered it). Fixed: `ContentRootPath = AppContext.BaseDirectory` (Program.cs, [REQ-FN-034]); publish rebuilt; re-verified styled from repo root + publish/mac (owner's cwd) + `dotnet run` — 4/4 assets 200, 7/7 specs, screenshot inspected. UsageGuide §7 row updated | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-11 | verify-phase `phase-mac` (verifier, ON the Mac, ledger docs/.last-verify.json) | MIXED — 7/7 UI specs + render/visual gates PASS on the styled :5999 board (mobile-390px caveat not reproduced); FN-008 detects live-correct 7/7; FN-027 re-confirmed. FOUND: FN-030 Catalyst cmd FAIL (NETSDK1100 on macOS), FN-031 + UI-001 → Needs re-verify (guide falsified; stuck-Pending row streaming), FN-028 detect can hang (gate chain) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-11 | Mac UAT-blocker fix (flow-master, on the Mac) | PASS — owner's "Web UI completely broken" root-caused: (1) the §3 Option A copy landed as 0-byte RDP placeholders (nothing runnable ever reached the Mac) and (2) the WSL `bin/Debug` output on the copied repo crashes on foreign machines (`UseStaticWebAssets` → DirectoryNotFoundException, Program.cs:25). Fixes: TrBlazeUI 1.0.7 nupkgs reconstructed into `~/LocalNuGet` on the Mac (real DLLs + static assets); Program.cs startup guard added; `publish/mac/TrSetup.Web` (osx-arm64, self-contained) built ON the Mac, booted, Playwright-smoked — styled board, live `mac.*` evidence, 0 errors. FN-008 live-smoked (stays Implemented — owner UAT §6.2 + verifier pending). UsageGuide §3/§7 updated (copy-verify step, Option A′ local publish, 2 new troubleshooting rows) | docs/TrSetup-Checklist.md#requirements-status |
| 2026-07-11 | fix-issues FN-030/UI-001/FN-028 + executed verify-phase re-verify (ON the Mac, ledger docs/.last-verify.json) | PASS — 3 root causes fixed (BoardView parameterless-child re-render skip + engine hard probe timeout; gate detect parallel + 3.5s/prereq bound; csproj TFM guard now replaces the list on OSX). 8/8 specs (suite now rejects "Pending"; new full-settlement test), 10/10 unit, catalyst-build4.log 0×NETSDK1100 (stops only at full-Xcode host state), screenshots inspected. UI-001 + FN-031 → Verified; FN-030 FAIL cleared → Implemented (Xcode UAT); FN-028 re-verified, working-dir defect open | docs/TrSetup-Checklist.md#requirements-status |

## Library feedback summary
- TrBlazeUI: 0 major, 1 minor (TR-001 — `Empty` component reference example vs shipped API) — docs/TrSetup-TrBlazeUI-Feedback.md
- TechieRag: 0 major, 0 minor — not used in this app

## Standards compliance (last check 2026-07-10, verify-phase inline)
- Underscore fields: CLEAN (src/, hand-written source)
- Test method underscores: CLEAN
- Mis-prefixed / snake_case fields & locals: CLEAN (renamed head files keep obj/a/v prefixes)
- Libraries free of Serilog references (NFR-007 boundary): CLEAN
- Build: 0 warnings / 0 errors (rung #4, full solution, 5 projects)

## Deferred / future
- The 8 UAT rows (per-host steps in docs/TrSetup-UsageGuide.md); owner sets `Released` after UAT
- Installer per-version SHA-256 checksums (REQ-FN-017 follow-up); confirm profile env-secret names against each app's canonical config keys when those repos onboard
