# TrSetup — Checklist

> Migrated from docs/TrSetup-Plan.md on 2026-07-05 (day1-greenfield §3.5 inline split). Phase structure (P1–P5, each independently shippable) carried over verbatim from the plan's §9; greenfield — nothing built yet, so every REQ starts `Not Started`/0%. Verify against docs/TrSetup-BRD.md before building.

## Table of Contents

1. [Goal](#goal)
2. [Requirements Status](#requirements-status)
3. [UI / Pages](#ui--pages)
4. [Functional requirements](#functional-requirements)
5. [RAG / AI requirements (→ /techierag)](#rag-ai-requirements-techierag)
6. [Non-functional](#non-functional)

## Goal

Replace the scattered, error-prone, three-environment manual setup (WSL / Windows host / LAN Mac) for the TechieFlow harness and the portfolio apps with one small app: open TrSetup → red/amber/green board for this machine's roles + the selected app → Fix / Fix all → it downloads, installs and configures what's missing itself → re-checks → green (BRD §1). One UI-free engine (`TrSetup.Core`), ONE head — the MAUI Blazor Hybrid desktop app (Win/Mac Catalyst) hosting the `TrSetupUI` RCL — and declarative per-app profiles. The head project was renamed `TrSetup.App` → `TrSetup` on 2026-07-10 (REQ-FN-035; the primary head carries the product name — `.App` is banned by the framework naming standard, 2026-07-10). *(2026-07-09 owner decision: the Blazor Server and Spectre.Console CLI heads are withdrawn — one single MAUI desktop app; decommission tracked by REQ-FN-034.)*

## Requirements Status

<!-- SINGLE SOURCE OF TRUTH for the WHOLE app. One row per REQ, grouped by phase.
     REQ-UI-* → /trblazeui (from docs/mockups/*) · REQ-FN-*/REQ-NFR-* → build-phase.
     No REQ-RAG-* — TrSetup has no AI features (BRD §15, Architecture ADR-003).
     Update Status + % + Remarks whenever build/smoke/verify touches a REQ. -->

| ID | Requirement | Status | % | Remarks | Details |
|----|-------------|--------|---|---------|---------|
| REQ-FN-001 | Check contract model (P1) | Verified | 100 | 2026-07-06 [REQ-FN-001] `Check` contract in TrSetup.Core (Id/Title/Category/Roles flags+NativeDev/Severity/Explain/DetectAsync/FixAsync?-null=manual/FixPreview/VerifyAsync); contract + NotApplicable-never-failure unit tests green | [view](#d-req-fn-001) |
| REQ-FN-002 | Detect→Preview→Fix→Re-verify pipeline (P1) | Verified | 100 | 2026-07-06 [REQ-FN-002] FixPipeline: consent-token gate (preview shown before grant), fix, VerifyAsync re-detect; non-green verify → FAILED + raw output attached; unit tests green incl. failed-verify case | [view](#d-req-fn-002) |
| REQ-FN-003 | Process runner with full output capture (P1) | Verified | 100 | 2026-07-06 [REQ-FN-003] IProcessRunner/ProcessRunner choke-point: exact command line + stdout/stderr + exit code + timeout kill, live per-line IProgress streaming; incremental-streaming unit test green | [view](#d-req-fn-003) |
| REQ-FN-004 | Role + app scoping of the check catalog (P1) | Verified | 100 | 2026-07-06 [REQ-FN-004] CheckEngine: roles∩app scoping (4 roles + NativeDev variant), out-of-scope rows NotApplicable, observable grouped CheckBoard, parallel sweep w/ 5s default probe timeout + streaming row updates; role/app-switch unit tests green | [view](#d-req-fn-004) |
| REQ-FN-005 | Local settings persistence (roles, endpoints, app) (P1) | Verified | 100 | 2026-07-06 [REQ-FN-005] JsonSettingsStore: roles/selected app/endpoints (MacIp) in %APPDATA%\TrSetup or ~/.trsetup/settings.json, test path override, missing file → first-run flag; save/reload + first-run unit tests green | [view](#d-req-fn-005) |
| REQ-FN-006 | WSL agent-host detects (P1) | Verified | 100% | 2026-07-06 [REQ-FN-006] Core/Catalog/Wsl: dotnet-sdk, headless-Chromium apt libs, winrun+PATH, node, playwright+chromium, mirrored-networking, git detects (F-WSLCHK). Ran LIVE — TrSetupReport-20260706-140549 shows 6✓/1✗ with real per-check evidence. Solution builds green (rung #4, 0W/0E). Verifier pending. | [view](#d-req-fn-006) |
| REQ-FN-007 | Windows device-host detects (P1) | Verified | 100% | 2026-07-06 [REQ-FN-007] Core/Catalog/Windows (11 checks): .wslconfig mirrored, Android SDK/API-34 image/Pixel_API_34 AVD, Node, Appium+uiautomator2, start-android-verify.ps1, :4723 session, MAUI workload, JDK (F-WINCHK), via winrun bridge. VERIFIED 2026-07-07 (flow-master inline): ran LIVE from this WSL box via the winrun/powershell interop bridge — all 10 in-scope win.* checks yield real Windows evidence (e.g. `SDKMANAGER-MISSING at C:\Users\srkra\AppData\Local\Android\Sdk\...`, `.wslconfig not found`, `:4723 connection refused`); bridge marker `probed via WSL interop bridge (powershell.exe)` present on every row. Acceptance ("detects run via the winrun bridge; each yields evidence") met. (The detects correctly report the tools as absent on this machine — that IS a working detect. FN-015 fixes remain destructive UAT.) | [view](#d-req-fn-007) |
| REQ-FN-008 | Mac device-host detects (P1) | Verified | 100 | 2026-07-21 RE-CONFIRMED (verify-phase phase-mac) — and this time the 7 F-MACCHK rows were read out of the **real Catalyst head's accessibility tree**, not only the web host, so the detect set is confirmed identical on the shipping head. Material change since the last run: `mac.appium-drivers` has flipped **Fail → Pass** ("Appium 3.5.2 with xcuitest@11.17.7, mac2@4.0.4 …"), independently corroborated because this run then drove a live mac2 session through that exact stack — the REQ-FN-016 repair is real, not a self-reported green. `mac.stable-ip` and `mac.appium-launchagent` still Fail and both remain **true** negatives (LAN is 192.168.64.2 vs configured 192.168.1.77; no `com.trsetup.appium.plist` in `~/Library/LaunchAgents`). Both acceptance halves (unit-tested detect logic 172/172 + executed live Mac run) continue to hold, so the row stays Verified. ⚠ One evidence-quality defect found this run affects the `mac.appium-launchagent` row's *message* (not its verdict) — a nested engine/probe timeout race that discards the URL-bearing diagnostic; logged in full under REQ-FN-016 and tracked as an open item. ‖ PRIOR: 2026-07-20 [REQ-FN-008] VERIFIER LIVE RUN (verify-phase phase-mac, on the Mac, ledger docs/.last-verify.json) — **acceptance fully met, promoted 90→100**. Both halves of the acceptance now hold: detect logic unit-tested with faked probes (149/149) AND the deferred live Mac run executed. All 7 F-MACCHK rows render real, evidence-bearing verdicts on the styled :5999 board: xcode-clt Pass (full Xcode /Applications/Xcode.app), dotnet-maui Pass, node Pass (v22.11.0), ios-simulator Pass (iOS 26.5), appium-drivers Fail, appium-launchagent Fail, stable-ip Fail (LAN 192.168.64.2 vs configured 192.168.1.77). All three Fails independently confirmed TRUE negatives, not false ones: `npm root -g` is TrSetup's own managed Node (`~/.trsetup/tools/node/...`) holding only corepack+npm — appium is genuinely absent globally (the server on :4723 is a repo-local node_modules binary from a prior agent session), and the LaunchAgent probe + stable-ip Fails follow from that plus real IP drift. Render gate (§4a) + visual gate (§4b, 1280x800 + 390x844, screenshots inspected) PASS. NOTE: the owner UsageGuide §6.2 walk-through is a separate UAT nicety, not part of this REQ's acceptance. ‖ PRIOR: 2026-07-11 [REQ-FN-008] VERIFIER LIVE RUN (verify-phase phase-mac, on the Mac "TechieMacPro", ledger docs/.last-verify.json): all 7 F-MACCHK detects computed REAL verdicts against this machine — xcode-clt Pass (CLT path), dotnet-maui Pass, node Pass (v26.5.0), appium-drivers Fail (exit 127, correct), stable-ip Fail (LAN 192.168.1.13 ≠ endpoint 192.168.1.50 + DHCP guidance, correct), ios-simulator Fail (no simctl, correct), appium-launchagent Fail ("Host is down" probing the configured endpoint, correct). tests/verify/phase-mac-ui.spec.ts REQ-FN-008 test PASS. ⚠ render gate (blocks Verified, defect owned by REQ-UI-001): on some loads the launchagent row (and a varying subset of others) stays "Pending" in the UI forever even though the engine computed its result (.verify/app2.log shows 5× "Check mac.appium-launchagent probe timed out after 00:00:05" with no matching row update). Detects themselves acceptance-met; owner UAT §6.2 still pending. ← 2026-07-11 LIVE MAC SMOKE (agent, on the owner's Mac "TechieMacPro"): board booted from a Mac-local osx-arm64 publish (UsageGuide §3 Option A′), roles Device host (Mac)+App runner (Mac) — `mac.*` rows returned REAL evidence, not blanks: Xcode/CLT **Pass** (CLT at /Library/Developer/CommandLineTools), Node **Pass** (v26.5.0), Appium **Fail** (not installed, exit 127 + real stderr), iOS Simulator **Fail** (no simctl), stable-IP **Warn** (no MacIp endpoint); screenshots inspected — styled render, 0 console errors, 0 asset 404s. Smoke only — owner UAT §6.2 + verifier still pending for Verified. ← 2026-07-06 [REQ-FN-008] Core/Catalog/Mac (7 checks): Xcode/CLT, .NET+MAUI, Node, Appium xcuitest+mac2, LaunchAgent :4723, stable-IP vs endpoint, iOS Simulator (F-MACCHK). Detect logic unit-tested; builds green. Live run deferred to a Mac session; verifier pending. | [view](#d-req-fn-008) |
| REQ-FN-009 | Cross-machine HTTP/ping probes (P1) | Verified | 100% | 2026-07-06 [REQ-FN-009] Core/Catalog/Probing (HttpStatusProbe/CrossMachineGuidance) + Bridges checks: HTTP/ping only, never remote-exec, 5s timeout, parallel; unreachable → Fail + "run TrSetup on <machine>" guidance. Report Bridges section rendered live. Verifier pending. | [view](#d-req-fn-009) |
| REQ-FN-010 | Report exporter — MD + HTML, secret-free (P1) | Verified | 100% | 2026-07-06 [REQ-FN-010] Core/Reporting (ReportExporter/ReportHtmlShell): full board→MD+HTML via shared shell. Ran LIVE — TrSetupReport-20260706-140549.md produced, secret-free. Builds green. Verifier pending. | [view](#d-req-fn-010) |
| REQ-FN-011 | Blazor Server head + `trsetup gui` launcher (P1) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: single MAUI desktop app — no Web head). Was Verified 2026-07-06. `TrSetup.Web` removal tracked by REQ-FN-034. | [view](#d-req-fn-011) |
| REQ-FN-012 | Spectre.Console TUI board renderer (P1) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: single MAUI desktop app — no CLI/TUI head). Was Verified 2026-07-07 incl. the UTF-8 glyph fix. `TrSetup.Cli` removal tracked by REQ-FN-034. | [view](#d-req-fn-012) |
| REQ-FN-013 | TUI keybindings f/a/r/e + arrows (P1) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: single MAUI desktop app — no CLI/TUI head). Was Verified 2026-07-07 incl. the terminal role/app picker + `trsetup config`. Removal tracked by REQ-FN-034. | [view](#d-req-fn-013) |
| REQ-UI-001 | Board dashboard screen (P1) | Verified | 100% | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): board dashboard: 3 specs PASS (grouped rows render; all rows settle to a real verdict across 2 fresh loads, none stuck Pending; §4a render sweep of every DevGuide control). ‖ PRIOR: VERIFIED 2026-07-11 (executed verify-phase, fix-issues re-verify, on the Mac "TechieMacPro", ledger docs/.last-verify.json): stuck-Pending streaming defect FIXED and the suite now enforces it — `tests/verify/phase-mac-ui.spec.ts` treats "Pending" as UNSETTLED and a new REQ-UI-001 full-settlement test (2 fresh loads) requires EVERY status badge ∈ {Pass, Fail, Warn, N/A}: 8/8 PASS on :5999 (board settled well inside the poll; historically-stuck rows asserted by id — mac.appium-launchagent → Fail with real probe evidence, appstudio.github-packages-feed → Warn (feed 401, PAT unset), appstudio.maccatalyst-build → Fail with real gate evidence). §4a render gate: all DevGuide controls render real data. §4b visual-truth: ✓ desktop 1280×800 AND ✓ mobile 390×844 (scrollWidth=390, NO horizontal overflow — old caveat stays cleared); test-results/phase-mac-board-{desktop,mobile}.png inspected (styled, no overlap/clip). 0 console errors, 0 failed requests. Engine hard-timeout + gate-budget covered by new unit tests (dotnet test 10/10). render+visual gate: all controls render and look right. ← 2026-07-11 [REQ-UI-001] FIX (builder agent, Mac): stuck-"Pending" rows had TWO root causes, both fixed. (1) UI: `BoardView.razor` (the component that actually renders the rows) had ZERO parameters and NO own `BoardState.Changed` subscription — Blazor's renderer skips re-rendering a retained parameterless child, so results landing after its last self-render were never painted (the pages inheriting `BoardComponentBase` re-rendered; the child didn't). Fixed: `BoardView.razor` now `@inherits BoardComponentBase` (own Changed subscription + unsubscribe via base Dispose; removed its duplicate `@inject BoardState/NavigationManager`). Audited the other razor files — no other parameterless child renders live row state (ConsentDialog subscribes to Consent.Changed itself; MainLayout renders no rows). (2) Engine: `CheckEngine.ProbeWithTimeoutAsync` relied on cooperative cancellation only — a check ignoring its token kept the row un-settled forever. Fixed: probe await hard-bounded with `.WaitAsync(vTimeoutCts.Token)` so the 5 s budget ALWAYS settles the row as Fail("Probe timed out…"). Files: src/TrSetupUI/Components/BoardView.razor, src/TrSetup.Core/Engine/CheckEngine.cs. Tests: tests/unit/TrSetup.Core.Tests Engine/CheckEngineProbeTimeoutTests (token-ignoring never-completing probe settles as timeout-Fail within ~the budget for sweep AND single re-check; fast rows unaffected) — full suite 10/10 green. NOTE: the historical 126-test suite is ABSENT from this Mac's working tree (tests/unit existed only in the sln reference; likely lost in the cross-machine copy) — the project was re-scaffolded at the sln path with the new tests; NOTE (verifier correction): `/tests` is GITIGNORED (line 19), so the historical suite was never committed — it lives only on the WSL machine's disk; owner restores by copying `tests/unit/TrSetup.Core.Tests` from the WSL clone (or un-ignoring `/tests`). Smoke (this Mac, :5999 Release web head, roles DeviceHostMac+AppRunnerMac, app AppStudio): 6 fresh board loads (2 scripted 3-run passes) — ZERO rows left "Pending"/"Checking" on every load, board fully settled in 1.1–5.7 s (was: stuck ≥90 s); the previously-stuck rows all settle with real evidence: mac.appium-launchagent → Fail ("Probe timed out after 5 s." on one load — the new hard bound painting; "Host is down (192.168.1.50:4723)" on the others), appstudio.github-packages-feed → Warn (feed reachable/401, PAT env var not set), appstudio.maccatalyst-build → Fail (real red-prereq list); 0 console errors, 0 HTTP≥400; desktop screenshot inspected — styled TrBlazeUI shell, colored status icons+words, evidence text on rows. ← 2026-07-11 [REQ-UI-001] DEMOTED (verify-phase phase-mac on the Mac, ledger docs/.last-verify.json): ⚠ render gate — INTERMITTENT STUCK-PENDING ROWS: after every board load a varying subset of rows never leaves "Pending" (observed stuck ≥90s across 4 independent loads: mac.appium-launchagent, appstudio.github-packages-feed, appstudio.maccatalyst-build always among them; once also maui-workload/dotnet-cli/xcode/appmanager-api). Engine PROVABLY computed some of the missing results (.verify/app2.log: 5× "Check mac.appium-launchagent probe timed out after 00:00:05", row still Pending) → row-update streaming loses late results (suspect: CheckEngine.DetectRowAsync→CheckBoard.ApplyResult→RowChanged→UI re-render path); separately appstudio.maccatalyst-build's detect re-runs its 8 prereq detects SEQUENTIALLY outside the engine's 5s-per-row budget (CheckCatalog.DetectRedIdsAsync) and can hang unboundedly (github-packages-feed check likely blocking — no PAT on this Mac). Screenshots test-results/phase-mac-board-desktop.png. Everything else PASSES: styled TrBlazeUI render, real evidence on settled rows, 0 console errors/404s, visual-truth ✓ desktop AND ✓ mobile 390px (rows now stack cleanly — the old 390px overflow caveat did NOT reproduce). The unstyled-boot-from-wrong-cwd finding was FIXED same day (content root pinned, see REQ-FN-034 FIX #2 — re-verified styled from 3 launch modes); the stuck-Pending streaming defect above remains the open item. ← 2026-07-06 [REQ-UI-001] Board from dashboard.html (TrBlazeUI shell): live 19 rows real Pass/Fail/Warn, group count badges, header role/app selectors re-scope w/o reload, per-group Fix all, Preview/Fix or Open guide (manual-only), Re-check all + Export. VERIFIED: Playwright smoke + visual gate desktop+mobile (render+visual pass). RE-CONFIRMED 2026-07-07 (flow-master inline, `*verify all`): desktop (1280×800) board renders all framework+bridges+**AppStudio profile rows** with real status icons/text/evidence and a clean, TrBlazeUI-styled layout — render-truth ✓ + visual-truth ✓ (screenshot inspected). ⚠ visual (minor, non-blocking): at 390px mobile the wide status-table overflows horizontally — per-row Fix/recheck buttons sit past the viewport (reachable via horizontal scroll; data all renders). TrSetup is a desktop/WSL/Mac dev tool so desktop is the real target; mobile responsive polish is a future item (not a library defect). RE-CONFIRMED 2026-07-10 (executed verify-phase): render+visual pass @ :5999, board-desktop screenshot inspected (29+ live rows, real evidence). | [view](#d-req-ui-001) |
| REQ-UI-002 | Check detail sheet (P1) | Verified | 100% | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): check-detail sheet deep link PASS — title, Explain, live evidence and Fix preview all populate. ‖ PRIOR: 2026-07-06 [REQ-UI-002] Sheet from check-detail.html over dimmed board: Explain+WORKFLOW link, detect evidence+timestamp, literal Fix preview w/ copy, collapsible last-run; deep link /check/{id} via FindRow. VERIFIED: smoke id=wsl.winrun, visual pass. RE-CONFIRMED 2026-07-07 (`*verify all`): /check/wsl.winrun deep link renders sections; render+visual pass. RE-CONFIRMED 2026-07-10 (executed verify-phase): deep-link render+visual pass. | [view](#d-req-ui-002) |
| REQ-UI-003 | First-run role picker (P1) | Verified | 100% | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): role picker PASS — role cards render. ‖ PRIOR: 2026-07-06 [REQ-UI-003] Picker from role-picker.html: 4 role cards + native-dev toggle + default-app select; Save→SaveSetupAsync→board; auto-shown first-run, Save disabled until ≥1 role. VERIFIED: genuine first-run smoke + visual pass. RE-CONFIRMED 2026-07-07 (`*verify all`): 4 role cards render at desktop+mobile; render+visual pass. RE-CONFIRMED 2026-07-10 (executed verify-phase): role cards render+visual pass. | [view](#d-req-ui-003) |
| REQ-UI-005 | Report preview screen (P1) | Verified | 100% | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): report preview PASS — board report renders. ‖ PRIOR: 2026-07-06 [REQ-UI-005] Preview from report.html: host/roles/app header + count badges + per-group evidence tables, secret-free footnote; Save MD/HTML via ReportExporter, Copy. VERIFIED: smoke group tables + no-secrets, visual pass. RE-CONFIRMED 2026-07-07 (`*verify all`): /report renders board report; render+visual pass. RE-CONFIRMED 2026-07-10 (executed verify-phase): report render+visual pass. | [view](#d-req-ui-005) |
| REQ-NFR-001 | Performance targets (P1, ongoing) | Verified | 100% | 2026-07-06 [REQ-NFR-001] Engine: parallel detect sweep (Task.WhenAll), 5s default per-probe timeout, streaming per-row updates, single-recheck path. VERIFIED 2026-07-07 (flow-master inline, measured): full detect sweep wall-time **5.79s** (target <30s ✓) — bounded by the 5s parallel-probe cap, so a single re-check is ≤5s (target <5s ✓); the board streams rows as they detect (Pending→Pass/Fail observed live) and the UI is never blocked by fixes (async pipeline). Meets the BRD §11 perf table. | [view](#d-req-nfr-001) |
| REQ-NFR-003 | Accessibility — keyboard nav, icon+text status (P1) | Verified | 100% | 2026-07-21 **FIXED + RE-VERIFIED** (fix-issues → verify re-run). Root cause was more specific than "wrong name": TrBlazeUI's icon package ships `lucide.json` with TWO maps — `icons` (1665 entries) and `aliases` (212) — and `LucideIcon` resolves from `icons` ALONE. `sliders` exists only as an **alias** (`{"parent":"sliders-vertical"}`), so it silently fell through to the error glyph. Changed to `sliders-horizontal`, a real `icons` entry. All 27 icon names across the UI were then validated against that map — literals plus the dynamic sources (`StatusLabel.IconName`, `FixRun.StepIcon`, `RoleCatalog` role icons); `sliders` was the only offender, and no alias-only names remain. REGRESSION GUARD ADDED (`tests/verify/phase-mac-ui.spec.ts`): asserts `page.content()` (raw DOM — NOT `innerText`, which is why 6 prior runs missed this) contains no `Icon not found:` across all 5 routes; falsification-tested by temporarily reverting the icon, which failed the guard on all 5 routes. RE-VERIFIED LIVE ON BOTH HEADS: web host `Icon not found` gone from the DOM and the Settings link's `innerText` is now exactly `"Settings"`; **native Catalyst head** accessibility tree shows the nav link's label restored to a clean `Settings` (was `"Icon not found: sliders Settings"`). Playwright 19/19, unit 176/176. ‖ PRIOR: 2026-07-21 ⚠ VISUAL/A11Y DEFECT FOUND (verify-phase phase-mac, first run driving the REAL Catalyst head over Appium mac2). `src/TrSetupUI/Layout/MainLayout.razor:49` renders `<LucideIcon Name="sliders" Size="16" />`, but `sliders` is not a valid Lucide icon name (upstream renamed it `sliders-horizontal`), so TrBlazeUI falls back to a literal error glyph. Consequences observed live on BOTH heads: (a) the Settings nav item renders a ⚠️ warning triangle where every sibling nav item renders its real icon — visible in `test-results/phase-mac-NATIVE-catalyst.png`; (b) **the accessible name of the Settings nav link becomes `"Icon not found: sliders Settings"`** — read straight out of the XCUITest tree, so a screen-reader user hears the error text before the link name. This is squarely this REQ's subject matter (icon+text status), hence the drop from Verified. Confirmed present in the web host's raw DOM too (`page.content()` contains `Icon not found: sliders`); it was missed by every prior run because the render sweep asserts on `innerText` + console errors, and this fallback surfaces in neither — no console error is emitted and the text lives in an attribute/glyph. FIX = one-word change to `sliders-horizontal`, plus a render-sweep assertion that no screen contains `Icon not found:`. Keyboard-nav and status icon+text halves re-confirmed passing (18/18 Playwright). ‖ PRIOR: 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): icon+text status re-confirmed live — every row renders "Pass"/"Warn"/"Fail" as text beside the icon, never colour alone. ‖ PRIOR: 2026-07-06 [REQ-NFR-003] Status = icon + text everywhere (StatusLabel), never color alone; role cards keyboard-navigable (tabindex + Enter/Space + aria-checked), focus-visible outlines, aria-modal overlays. VERIFIED in board/setup screenshots (render+visual). | [view](#d-req-nfr-003) |
| REQ-NFR-005 | Testability ids — data-testid / AutomationId (P1, ongoing) | Verified | 100% | 2026-07-06 [REQ-NFR-005] Stable intent-named data-testid on every interactive/data-bound Blazor control (board-row-{id}, status-{id}, fix-all-{group}, RolesSelector, role-card-{key}, fix-step-{id}, consent-{id}, ReportPreview…); --check --json agent surface (FN-032). VERIFIED: Playwright smoke drove pages by testid. MAUI AutomationId deferred to the P4 head (FN-030). | [view](#d-req-nfr-005) |
| REQ-FN-014 | WSL auto-fixers (P2) | Implemented | 75% | 2026-07-06 [REQ-FN-014] Fixers on F-WSLCHK checks: dotnet-install SDK, Node LTS (+~/.bashrc managed PATH block), winrun write+chmod+PATH, Playwright install, apt libs + git via sudo terminal-handoff. Idempotent; manual rows FixAsync=null. 8 fixer unit tests green; FixPreview renders live in board/detail. Live install = destructive UAT (not smoked). | [view](#d-req-fn-014) |
| REQ-FN-015 | Windows auto-fixers (P2) | Implemented | 75% | 2026-07-06 [REQ-FN-015] Fixers on F-WINCHK: .wslconfig managed-block + wsl --shutdown prompt, cmdline-tools/SDK/API-34/AVD, Node MSI + Temurin JDK via visible UAC child, Appium+uiautomator2, ps1 from embedded template, MAUI workload. Elevated steps require granted ConsentToken (declined=nothing runs). 9 unit tests green. Live install = UAT. | [view](#d-req-fn-015) |
| REQ-FN-016 | Mac auto-fixers (P2) | Implemented | 96 | 2026-07-21 **TIMEOUT RACE FIXED + RE-VERIFIED (fix-issues).** The nested-timeout defect logged earlier today is closed: `CheckEngine.DefaultProbeTimeout` was 5 s and `HttpStatusProbe.ProbeTimeout` was ALSO 5 s, so the engine's `CancelAfter` expired at the same instant as a check's own HTTP probe and its `catch (OperationCanceledException)` returned the generic `"Probe timed out after 5 s."`, discarding the check's own message. `DefaultProbeTimeout` is now **derived** as `HttpStatusProbe.ProbeTimeout + 3 s` (8 s) rather than hard-coded, so the two can never silently re-equalise, with an XML comment recording why. The deliberate `WaitAsync` hard bound is untouched, so the REQ-UI-001 hang fix does not regress (pinned by a dedicated test). LIVE PROOF on the board: the `mac.appium-launchagent` row now reads **"Appium not answering on http://192.168.1.77:4723/status (TaskCanceledException: … HttpClient.Timeout of 5 seconds elapsing.)"** — the stale configured IP is finally visible, where before the row said only "Probe timed out after 5 s." Zero occurrences of the generic string remain. +4 unit tests (`CheckEngineProbeEvidenceTests`) covering "inner probe fails just under budget → check's OWN evidence survives" and "check hangs forever → still settles as an engine timeout"; suite 172 → **176**. Ceiling stays Implemented — the one remaining acceptance half, **LaunchAgent survives reboot**, still needs a reboot (owner UAT) and the plist is still deliberately not installed. ‖ PRIOR: 2026-07-21 [REQ-FN-016] VERIFIER RUN — the repaired Appium stack is now proven **functional, not merely detected**. This verify run created a real mac2 WebDriver session through the very stack this fixer installs (`APPIUM_HOME=~/.trsetup/tools/appium`, appium 3.5.2 + mac2@4.0.4, Node v22.23.1) and drove the Catalyst head end-to-end — which is the "NOT yet exercised: a real Mac2 WebDriver *session* against a Catalyst app" item the prior entry left open. Live board row re-confirmed Pass with evidence "Appium 3.5.2 with xcuitest@11.17.7, mac2@4.0.4 in APPIUM_HOME=/Users/sravikumar/.trsetup/tools/appium (server and drivers mutually compatible)"; mac.node Pass v22.23.1. Acceptance half "manual-only rows never grow a Fix button" re-confirmed. Remaining gap is unchanged and owner-bound: **LaunchAgent reboot-survival** — `~/Library/LaunchAgents/` still contains NO `com.trsetup.appium.plist` (deliberately never installed; the running :4723 server was started by hand), so `mac.appium-launchagent` correctly Fails. ⚠ SEPARATE DEFECT LOGGED THIS RUN (evidence quality, not verdict): that row's evidence renders as the contentless string "Probe timed out after 5 s." instead of the check's own URL-bearing message (`MacAppiumLaunchAgentCheck` builds "Appium not answering on http://{MacIp}:4723/status (...)"). Root cause is a **nested-timeout race** — `.verify/app.log` shows `CheckEngine[0] Check mac.appium-launchagent probe timed out after 00:00:05`, i.e. the engine's 5 s per-check budget expires at the same instant as the check's own 5 s HTTP probe, so the engine cancels and substitutes a generic message, discarding the single most useful diagnostic (WHICH address was probed — here the stale `192.168.1.77`, while the server answers on loopback in 6 ms). Any check whose internal probe timeout equals the engine budget can never render its own failure evidence. FIX = give the engine a budget strictly greater than the probe timeout (or let a check's own evidence win the race). Ceiling stays Implemented pending the reboot check. ‖ PRIOR: 2026-07-21 [REQ-FN-016] APPIUM FIXER REPAIRED + SELF-SMOKED on the live Mac; also unblocks REQ-FN-030 (Catalyst automation was impossible because Appium was broken). FIVE real defects, each reproduced before fixing. (1) **Non-deterministic driver detection** — `APPIUM_HOME` was never set, so Appium resolved its extension manifest from the PROCESS CWD (same defect class as the REQ-FN-028 "working dir resolves to process cwd" bug). Live proof: two divergent manifests, `<repo>/node_modules/.cache/appium` holding mac2@4.0.4 and `~/.appium` holding mac2@2.2.2 — the fixer could write one while the detect read the other. Detect AND fixer now both export a single pinned `APPIUM_HOME=~/.trsetup/tools/appium`; re-ran detect from `/Users/MyCode/TrSetup`, `$HOME` and `/tmp` — byte-identical Pass evidence from all three. (2) **Appium stuck at 2.0.1 vs registry latest 3.5.2** — ROOT CAUSE was NOT npm/registry: appium@3 declares engines `^20.19.0 or ^22.12.0 or >=24.0.0` and `mac.node` pinned Node **v22.11.0**, which satisfies none of them, so npm silently resolved the newest engine-compatible Appium (2.0.1) while the current Apple drivers peer-require `^3.0.0-rc.2` — a permanently mismatched pair. Node pin raised to **v22.23.1** (+ new `MinimumNodeVersion` v22.12.0 that fails the row honestly instead of passing a Node that dooms Appium), and the server + both drivers are now pinned as one mutually compatible set (appium@3.5.2 + xcuitest@11.17.7 + mac2@4.0.4) with a comment saying they move together. (3) **`xcuitest` never landed** — the "chain aborted" story is FALSE: I reproduced `appium driver install xcuitest` succeeding under 2.0.1 (it resolves 9.10.5). The real cause was (1): fixer writes and detect reads scattered across cwd-dependent homes, and neither surviving manifest was the product of a complete fixer run. Guard rewritten anyway — it was `grep -q <name>` over stdout+stderr, which (a) matched driver names inside Appium's own "may be incompatible" WARNING lines and (b) treated an OLD incompatible driver as "already installed", so a version-mismatched manifest could never be repaired. Now parses `driver list --installed --json` and guards on the exact pinned VERSION (uninstall→install to converge; true no-op when already correct). (4) **Evidence conflated "no Appium" with "Appium present, drivers wrong"** — detect now reports three distinct failures (SERVER NOT FOUND / drivers MISSING / drivers INCOMPATIBLE) and, critically, **refuses to Pass a server+driver major mismatch** (the live 2.0.1-vs-mac2@4.0.4 state the old check would have called green). (5) **NEW, found while smoking** — `mac.node`'s fixer extracted the tarball ON TOP of the existing install; `tar` never deletes, so the bumped pin left 9 stale dirs inside npm's own node_modules and npm died on every command with "Class extends value undefined is not a constructor or null" — the Node fixer bricking npm for the Appium fixer that depends on it. Now clears the dir, and clears it BEFORE the download (the downloader stages the archive INSIDE that dir — clearing after deleted the tarball and left the Mac with no Node at all; both orderings observed live and both are now regression-tested). BONUS: the `mac.appium-launchagent` plist — the server that ACTUALLY SERVES the drivers — set neither `APPIUM_HOME` (so the served Appium loaded a cwd-relative driver set, making a green board row a lie) nor `PATH` (`bash -lc` reads `~/.bash_profile`, never the `~/.zprofile` block `mac.node` writes, so the managed Node was invisible to launchd); plist now pins both. SMOKE (self-run, real machine, no fakes — harness drove the REAL `MacNodeCheck`/`MacAppiumDriversCheck`/`MacAppiumLaunchAgentCheck` classes through the real `ProcessRunner`): mac.node fixer re-installed Node v22.23.1 → Appium fixer converged `~/.trsetup/tools/appium` onto appium@3.5.2 + xcuitest@11.17.7 + mac2@4.0.4 → fixer re-run is a true no-op reporting success ("skipped, already at ...") → `appium` started and `curl http://127.0.0.1:4723/status` returned `{"ready":true,...,"build":{"version":"3.5.2"}}` on BOTH loopback and the LAN address, with XCUITestDriver + Mac2Driver logged as loaded from the pinned home, launched with `env -i` carrying ONLY the generated plist's env from cwd `/` (worst case for the old bug); generated plist passes `plutil -lint`. Board row confirmed in TrSetup.Web (headless Playwright, `tests/verify/reqfn016-appium.spec.ts`): **mac.appium-drivers = Pass** with evidence "Appium 3.5.2 with xcuitest@11.17.7, mac2@4.0.4 in APPIUM_HOME=/Users/sravikumar/.trsetup/tools/appium (server and drivers mutually compatible)", mac.node = Pass v22.23.1; render-truth + visual-truth (geometry + eyeballed screenshot) both pass — screenshots `test-results/reqfn016-board.png`, `test-results/reqfn016-appium-row.png`. Servers stopped afterwards. Unit suite **172 green** (was 149; +23, incl. 11 new REQ-FN-016 tests covering cwd-independence, the three distinct failure modes, the warning-text false positive, pinned-set install, wrong-version convergence, no-op re-run, the Node minimum, and both `tar`-overlay/ordering regressions). Ceiling is Implemented — self-smoke is not verify. NOT yet exercised: a real Mac2 WebDriver *session* against a Catalyst app (that is REQ-FN-030's job, now unblocked), and LaunchAgent reboot-survival (needs a reboot → owner UAT); the LaunchAgent was deliberately NOT installed into `~/Library/LaunchAgents` — plist content was smoke-tested to a temp path instead so no permanent agent was added to the owner's Mac. ‖ PRIOR: 2026-07-20 [REQ-FN-016] VERIFIER PARTIAL (verify-phase phase-mac, live Mac board). Acceptance half 1 — "manual-only rows never grow a Fix button" — RUNTIME-CONFIRMED: manual rows render **Open guide** with no Fix (mac.stable-ip, appstudio.appmanager-api, appstudio.appmanager-secret) while fixable rows render **Fix** (mac.appium-drivers, mac.appium-launchagent, appstudio.maccatalyst-build); screenshot test-results/phase-mac-board-desktop.png. Acceptance half 2 — "LaunchAgent survives reboot" — NOT OBSERVABLE (needs a reboot) → owner UAT. Fix paths themselves remain unexecuted by design (the verify suite is read-only and never clicks Fix — fixes install software). Stays Implemented until owner UAT. ‖ PRIOR: 2026-07-20 [REQ-FN-016] .NET PATH BLOCK ADDED (flow-master, Mac) — closes the follow-up logged with the sibling audit. `mac.node` wrote a `~/.zprofile` managed block; `mac.dotnet-maui` wrote NONE, so TrSetup could install a .NET SDK the owner could never invoke from their own terminal (`dotnet-install.sh` deliberately installs a PRIVATE SDK that is on no PATH at all) — a tool whose promise is "installs everything" leaving the install unusable. FixCoreAsync now upserts managed block `mac.dotnet-path` exporting BOTH `DOTNET_ROOT` and `PATH` (dotnet-install.sh documents both: DOTNET_ROOT alone will not run `dotnet`, PATH alone leaves DOTNET_ROOT unset for tools that read it), mirroring MacNodeCheck exactly — same ManagedBlockWriter, same CommentSyntax.Hash, same injectable zprofile resolver. FixPreview and the detect evidence both now name the block and the new-login-shell caveat. Files: src/TrSetup.Core/Catalog/Mac/MacDotnetMauiCheck.cs. Tests: +2 (idempotent double-run leaves EXACTLY ONE block and exports both vars; pre-existing user content in the profile survives byte-for-byte) — suite 149/149. ⚠ TEST-HYGIENE DEFECT FOUND AND FIXED IN THE SAME CHANGE: the pre-existing DotnetMauiFixDownloadsSdkThenInstallsWorkload test passed no zprofile resolver, so once the fixer started writing a block that test wrote into the REAL `~/.zprofile` of whoever ran the suite (observed live — it added a mac.dotnet-path block to this machine's profile). Now injects a temp path; verified by diffing the real ~/.zprofile across a full suite run (UNCHANGED). Any future fixer test that touches a user-home file must inject its path. Playwright 15/15; mac `.app` + web builds green. NOT Verified: no verify-phase run. ← 2026-07-20 [REQ-FN-016] SIBLING AUDIT COMPLETE — post-fix PATH blindness swept across ALL 5 Mac fixers (owner request). **Result: 2 more affected, 3 clean.** (a) `mac.dotnet-maui` AFFECTED — its own fixer installs the SDK into `~/.dotnet` and even invokes `{DotnetDir}/dotnet` ABSOLUTELY for the workload step, but DetectAsync probed bare `dotnet workload list`; a successful install therefore re-verified red. Fixed with the same managed-location fallback + honest evidence naming the path and warning that `dotnet` will not resolve in a terminal (the .NET fixer, unlike mac.node's, writes NO PATH block at all — logged below). (b) `mac.appium-drivers` AFFECTED TWICE — DetectAsync probed bare `appium`, AND the fixer shelled bare `npm`, so on a machine TrSetup itself provisioned (Node under the managed tools root, exported only from ~/.zprofile) TrSetup installed Node and then could not use it. Fixed: detect falls back to `<managedNodeBin>/appium`, and the fix PREPENDS the managed bin dir to PATH (prepend not replace, so a system/Homebrew Node is unaffected). New shared accessor `MacNodeCheck.ManagedNodeBinDir` keeps one source of truth. (c) CLEAN, no change needed: `mac.xcode-clt` (`xcode-select`), `mac.ios-simulator` (`xcrun`) and `mac.appium-launchagent` (`launchctl` + HTTP) all shell SYSTEM binaries in /usr/bin that are always on PATH. ⚠ ROOT-CAUSE INSIGHT (why this class hid for so long): `dotnet TrSetup.Web.dll` is MUXER-hosted and a muxer-hosted process can spawn `dotnet` even when it is ABSENT from PATH, whereas the shipping MAUI head is a self-contained apphost and cannot. Proved with a purpose-built probe (scratchpad, macOS 26.5, PATH=/usr/bin:/bin:/usr/sbin:/sbin): identical code, muxer-hosted => `Process.Start("dotnet")` OK; self-contained apphost => Win32Exception 'No such file or directory'. **The web smoke host is therefore NOT a faithful proxy for the MAUI head on anything PATH-related** — verify PATH-sensitive rows on the real head. Tests: +5 in MacFixerTests pinning all three fallbacks (node managed-install detect; node still honestly Fails when neither PATH nor managed install exists; dotnet managed-SDK detect via new `DotnetDirOverride` seam; appium managed-bin detect; appium fix prepends the managed bin dir) — suite 147/147 (was 142). Playwright 15/15. Mac `.app` + web builds green. NOT Verified: no verify-phase run. FOLLOW-UP LOGGED: the .NET fixer should write a `~/.zprofile` managed PATH block like mac.node does — today TrSetup can install .NET that the user can never invoke from their own terminal. ← 2026-07-20 [REQ-FN-016] FIX (flow-master, Mac, clean machine) — **"clicking Fix does nothing" (owner-reported) root-caused and fixed** for mac.node. The fixer was in fact SUCCEEDING (downloaded + extracted Node v22.11.0 into ~/.trsetup/tools/node, wrote the `mac.node-path` managed block into ~/.zprofile), but `MacNodeCheck.DetectAsync` probed only bare `node --version` off the RUNNING PROCESS's PATH. ~/.zprofile is sourced by LOGIN SHELLS ONLY, so the immediate `FixPipeline` re-verify still failed, the row stayed red, and a completed 232 MB install was indistinguishable from a no-op. Fixed: detect now falls back to probing the managed binary directly (`~/.trsetup/tools/node/bin/node --version`) and, on success, Passes with honest evidence that names the managed path and tells the user a NEW LOGIN SHELL is needed before bare `node` resolves. File: src/TrSetup.Core/Catalog/Mac/MacNodeCheck.cs. Smoke (this Mac, published :5999): mac.node row went Fail → **Pass** — "Node.js v22.11.0 present at /Users/sravikumar/.trsetup/tools/node/bin/node (TrSetup-managed install). Not yet on this process's PATH…". Playwright spec REQ-FN-008 (mac detects: real status + evidence) went red → green; full suite 15/15. NOT Verified: no verify-phase run executed this session. Sibling fixers (appium-drivers, ios-simulator, appium-launchagent) may share the same post-fix-PATH blindness — not yet audited. ← 2026-07-11 [REQ-FN-016] verify-phase phase-mac (Mac): fixer SURFACES confirmed live read-only — failing fixable rows (mac.appium-drivers, mac.ios-simulator, mac.appium-launchagent) render Fix buttons + previews; manual-only rows correctly show Open guide instead. Destructive live installs NOT run by the verifier (policy) — owner UAT §6.2 step 2 remains the promotion path. ← 2026-07-06 [REQ-FN-016] Fixers on F-MACCHK: xcode-select CLT (full Xcode stays manual), SDK+MAUI workload, Node (+~/.zprofile managed PATH), Appium xcuitest/mac2, LaunchAgent plist managed block + launchctl load, -downloadPlatform iOS; DHCP reservation stays manual. 6 unit tests green. Live run = Mac UAT. | [view](#d-req-fn-016) |
| REQ-FN-017 | Installer download framework — pinned URLs, checksums, managed locations (P2) | Verified | 100% | 2026-07-06 [REQ-FN-017] InstallerDownloader + TrSetupPaths managed root (Downloads/): pinned URL verbatim in FixPreview, SHA-256 verify, tampered payload deleted+reported, "no published checksum" recorded. VERIFIED 2026-07-07 (flow-master inline): acceptance ("tampered download fails checksum + reports; install paths under the managed root") asserted by passing unit tests incl. a **real temp-file checksum-mismatch download** — re-run green this pass (part of the 23-test Downloader/ConfigWriting/FixAll/Elevation acceptance run). | [view](#d-req-fn-017) |
| REQ-FN-018 | Idempotent config-write framework (marker blocks) (P2) | Verified | 100% | 2026-07-06 [REQ-FN-018] ManagedBlockWriter (ConfigWriting/): upsert/remove marker blocks for # ; // <!-- --> syntaxes, re-run = single block replaced in place, user edits outside markers preserved byte-for-byte, missing file created. VERIFIED 2026-07-07 (flow-master inline): acceptance ("run fix twice → single block; hand-edit outside block survives a re-fix") asserted by passing **real-file round-trip** unit tests — re-run green this pass. | [view](#d-req-fn-018) |
| REQ-FN-019 | Fix-all dependency-ordered runner (P2) | Verified | 100% | RECONCILED 2026-07-09: status column lagged its own evidence (row already carried "VERIFIED 2026-07-07 flow-master inline"). 2026-07-06 [REQ-FN-019] FixStep/FixAllPlanner/FixAllRunner (FixAll/): stable topo sort on DependsOn, per-step consent→fix→re-verify via FixPipeline, declined consent halts run with later steps untouched, stop/continue-on-failure policy, IProgress step streaming. VERIFIED 2026-07-07 (flow-master inline): acceptance ("order verified by unit test on the dependency graph; declined consent halts with later steps untouched") asserted by passing unit tests (Node→Appium, SDK→AVD topo graph) — re-run green this pass. | [view](#d-req-fn-019) |
| REQ-FN-020 | Elevation/consent runner — UAC child, sudo handoff (P2) | Verified | 100% | RECONCILED 2026-07-09: status column lagged its own evidence (row already carried "VERIFIED 2026-07-07 flow-master inline"). 2026-07-06 [REQ-FN-020] ElevationRunner (Elevation/): UAC via powershell Start-Process -Verb RunAs visible child through IProcessRunner, granted ConsentToken required (declined throws, nothing launched), WSL sudo = TerminalHandoff print-to-paste, no password code path. VERIFIED 2026-07-07 (flow-master inline): acceptance ("no code path stores or caches credentials; every elevated run is user-initiated") asserted by passing unit tests (granted-ConsentToken-required, declined-throws-nothing-launched, no-password path) — re-run green this pass. | [view](#d-req-fn-020) |
| REQ-UI-004 | Fix-all run view with consent gates (P2) | Verified | 100% | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): fix-run view PASS — renders (no Fix executed; suite is read-only by design). ‖ PRIOR: 2026-07-06 [REQ-UI-004] Fix-run from fix-run.html: dependency-ordered step list (9 steps), per-step status, inline consent gate (exact command + Approve/Decline), halt-on-decline, summary; drives FixAllRunner via new BoardState fix-all method. VERIFIED: consent gate blocks execution (nothing ran) + empty state, visual pass. RE-CONFIRMED 2026-07-07 (`*verify all`): /fix-run renders; render+visual pass. RE-CONFIRMED 2026-07-10 (executed verify-phase): fix-run render+visual pass. | [view](#d-req-ui-004) |
| REQ-NFR-002 | Security stance — presence-only secrets, outbound-only, no telemetry (P2; stance from P1) | Verified | 100% | 2026-07-06 [REQ-NFR-002] framework level; fixer wiring in FN-014..016 — consent-per-elevation (granted ConsentToken mandatory), pinned sources + SHA-256 checksums, grep-proof no credential storage (sudo = terminal handoff, UAC = visible child) | [view](#d-req-nfr-002) |
| REQ-NFR-004 | Reliability — failed fix leaves machine no worse (P2) | Verified | 100% | 2026-07-06 [REQ-NFR-004] framework level; fixer wiring in FN-014..016 — temp-file downloads (tampered/failed payloads deleted, never half-written), idempotent marker-block config writes, all installs under TrSetup-managed root so re-runs always safe | [view](#d-req-nfr-004) |
| REQ-FN-021 | Profile schema + loader + app-repo override merge (P3) | Verified | 100 | 2026-07-07 [REQ-FN-021] TrSetup.Core/Profiles: TrSetupProfile/ProfileRequirement model (10 typed requirement instances, role-tagged), ProfileLoader built-in (embedded JSON auto-discovery keyed by name) + `.tfcore/trsetup-profile.json` override merge (app-repo-wins by requirement Id), schema validation (ProfileRequirementParamRules), IProfileRequirementHandler registry + ProfileCheckFactory → Check, CheckCatalog append guarded (no app/failed load → appends nothing, never throws). 13 unit tests (conflict-merge app-repo-wins, role-tag survival, unknown-type placeholder, AppliesTo scoping). E2E: both built-in profiles load+render via `trsetup --check --json`. | [view](#d-req-fn-021) |
| REQ-FN-022 | AppStudio built-in profile (P3) | Verified | 100 | 2026-07-20 [REQ-FN-022] VERIFIER RE-VERIFY PASS (verify-phase phase-mac, live Mac board) — **clears `Needs re-verify`**. Acceptance ("selecting AppStudio renders exactly these rows scoped to the machine's roles") met: with SelectedApp=AppStudio and roles DeviceHostMac+AppRunnerMac the board renders exactly the F-PROFILES set — dotnet-sdk Pass (10.0.302), maui-workload Pass, dotnet-cli Pass, git-cli Pass, **xcode Pass ("xcodebuild -version" → Xcode 26.6 Build 17F113)**, github-packages-feed Warn (feed reachable, GITHUB_PACKAGES_PAT unset), appmanager-api Fail, appmanager-secret Fail. The 2026-07-20 Xcode false-negative fix (`-version` not `--version`) is confirmed live — the row that reported absent now detects the installed Xcode. Render + visual gates PASS. ‖ PRIOR: 2026-07-20 [REQ-FN-022] FIX (flow-master, Mac, clean machine) — **false-negative Xcode detect, found by owner challenge ("I have Xcode installed").** `appstudio.xcode` declared `params: { command: "xcodebuild" }` and so inherited `CliToolRequirementHandler`'s default `versionArgs` of `--version`. xcodebuild is an Apple tool that REJECTS the GNU double-dash form: `$ xcodebuild --version` → exit 64 (EX_USAGE) + an .xcresult error bundle, which the handler reports as "'xcodebuild' not found or failed". Net effect: a full, working Xcode 26.6 detected RED and, being an AppRunnerMac prerequisite, gated the entire REQ-FN-028 Catalyst build path — the tool told the owner to install something they already had. Fixed in the profile (the handler already supported the knob): `params: { command: "xcodebuild", versionArgs: "-version" }`. No code change needed. File: src/TrSetup.Core/Profiles/BuiltIn/appstudio.json. Test: BuiltInProfilesTests.AppStudioXcodeRowUsesAppleSingleDashVersionFlag (pins command + versionArgs so the default can never silently reclaim this row) — suite 142/142. Smoke (this Mac, published :5999): appstudio.xcode Fail → **Pass** "'xcodebuild' present ($ xcodebuild -version): Xcode 26.6 Build version 17F113"; the FN-028 gate stopped listing it. Status demoted from Verified because the row's definition changed this session and no verify-phase run was executed. Audit note: appstudio/trstudio are the only cli-tool rows; dotnet + git both accept `--version`, so no sibling row is affected. ← 2026-07-07 [REQ-FN-022] appstudio.json (8 rows: .NET SDK, MAUI workload, dotnet+git PATH split atomic, Xcode Mac-runner, techierathore GitHub Packages feed, AppManager endpoint, AppManager key presence-only), roles Win→DeviceHostWindows / Mac-runner→AppRunnerMac. BuiltInProfilesTests + E2E render-truth: all 8 rows detect live (dotnet-sdk/git pass, maui-workload/xcode/appmanager-api fail correctly, secret fail presence-only, feed warn) — no blank evidence. | [view](#d-req-fn-022) |
| REQ-FN-023 | TrStudio built-in profile (P3) | Verified | 100 | 2026-07-07 [REQ-FN-023] trstudio.json (10 rows: .NET SDK, Postgres+PgVector service, ffmpeg service, isolated Python+ComfyUI runtime-install, disk-space floor Recommended, TechieRag NuGet PAT feed Win-only, RunPod/HeyGen/AppManager keys split atomic presence-only, AppManager endpoint). ffmpeg modeled as `service` (gets FN-026 install fixer; board title still "ffmpeg on PATH"). BuiltInProfilesTests + E2E: all heavy rows detect live (postgres/ffmpeg/comfyui real probe evidence, disk-space pass 936.9 GB free/80 GB floor). | [view](#d-req-fn-023) |
| REQ-FN-024 | core-config appium-block writer + head curl-verify (P3) | Verified | 100 | 2026-07-07 [REQ-FN-024] AppiumConfigBlockCheck (Catalog/Framework, AgentHostWsl-scoped, NotApplicable outside an app repo): writes `runtimeVerification.appium` block (WORKFLOW §0b step-4 shape — android always, ios/maccatalyst when MacIp set) via ManagedBlockWriter idempotent markers (user config outside markers preserved), then curl-verifies each head `url+/status`. AppiumConfigBlockCheckTests: round-trip + re-run single block + curl-verify vs fake probe. E2E: row renders. | [view](#d-req-fn-024) |
| REQ-FN-025 | Isolated Python + ComfyUI runtime-install fixer (P3) | Implemented | 75 | 2026-07-07 [REQ-FN-025] RuntimeInstallCheck/RuntimeInstallRequirementHandler: detects ComfyUI entrypoint under `TrSetupPaths.ToolsRoot/comfyui`, fix downloads pinned official ComfyUI portable release (bundled Python — no system-Python collision) into managed location; Explain states BRD-39 boundary (TrStudioAdmin owns models/workflows above runtime). RuntimeInstallHandlerTests (detect-absent, preview pinned-URL+managed-path) + E2E detect renders real evidence. Live download+install = destructive UAT (not run). | [view](#d-req-fn-025) |
| REQ-FN-026 | Postgres+PgVector + ffmpeg service fixers (P3) | Implemented | 75 | 2026-07-07 [REQ-FN-026] ServiceCheck/ServiceRequirementHandler (service∈{postgres,ffmpeg}): postgres detect psql+pg_extension vector, fix winget/brew install + `CREATE EXTENSION IF NOT EXISTS vector` (idempotent); ffmpeg detect `ffmpeg -version`, fix winget/brew. Preview shows both winget+brew literals (host resolved at fix-time via injectable selector). ProfileHeavyHandlerTests + E2E detect renders real evidence. Live install = destructive UAT (not run). | [view](#d-req-fn-026) |
| REQ-FN-027 | Mac app-runner role wiring (P3) | Verified | 100 | 2026-07-20 [REQ-FN-027] RE-CONFIRMED (verify-phase phase-mac, live Mac): spec "REQ-FN-027 Mac app-runner aggregation" PASS — under DeviceHostMac+AppRunnerMac with AppStudio the board aggregates the profile requirement rows plus the Catalyst build row; render + visual gates PASS. ‖ PRIOR: 2026-07-11 [REQ-FN-027] runtime render-confirmed on the Mac (verify-phase phase-mac): under roles DeviceHostMac+AppRunnerMac with AppStudio, the board aggregates all 8 appstudio.* profile rows + the appstudio.maccatalyst-build fixer row (tests/verify/phase-mac-ui.spec.ts REQ-FN-027 test PASS) — aggregation/scoping correct. ⚠ status cells of some aggregated rows are hit by the board streaming defect recorded under REQ-UI-001 (defect owned there, not an aggregation fault). ← 2026-07-07 [REQ-FN-027] AppRunnerMac aggregation via engine role∩app scoping over A's profile append + the Catalyst fixer row — no RoleCatalog change needed (option pre-existed). MacAppRunnerAggregationTests (roles=AppRunnerMac + SelectedApp=AppStudio → exactly the 8 Mac-runner-tagged requirements + maccatalyst-build). E2E confirmed: AppStudio board under AppRunnerMac aggregates all profile rows + build fixer. | [view](#d-req-fn-027) |
| REQ-FN-028 | "Build & install <App> for Mac (Catalyst)" fixer (P3) | Implemented | 90 | 2026-07-21 [REQ-FN-028] FIX (Mac, live) — the App Manager endpoint is now OVERRIDABLE PER MACHINE, which is what unblocked the Catalyst fixer's first prerequisite. Root cause: appstudio.json declared `appstudio.appmanager-api` as the hardcoded `https://localhost:5101/` while scoping that SAME requirement to BOTH DeviceHostWindows AND AppRunnerMac — a self-contradiction. On a real two-machine setup the Mac app-runner has nothing on its own localhost:5101 (App Manager runs on the Windows device-host, confirmed live at 192.168.1.14:5101), so the row could NEVER go green and the fixer stayed gated behind a prerequisite the user had no way to satisfy. **Gating logic deliberately untouched.** Fixed via the EXISTING settings idiom, not a parallel config mechanism: an `endpoint` requirement may now declare an optional `urlSettingKey` param naming a `TrSetupSettings.Endpoints` key that REPLACES its default URL on this machine (appstudio.json → `urlSettingKey: AppManagerUrl`); new `EndpointResolver.Resolve` returns `{Url, IsOverridden, AllowSelfSignedCertificate, Source}`, resolved PER DETECT (not once at CreateCheck) so a Save is seen by the very next sweep with no restart. Evidence now always names the URL AND its provenance, so `connection refused (localhost:5101)` can no longer be misread as 'the service is down' when the real cause is 'pointed at the wrong host'. Surfaced in the UI (no JSON editing): Settings gains an `AppManagerUrl` field — `EndpointField` grew a `Kind` (Address|Url) so URL fields validate as absolute http(s) via `IsValidUrl` instead of the hostname regex — plus a hint line. **TLS DECISION (deliberate, documented, opt-in):** the LAN App Manager serves the ASP.NET dev cert (verified live: `subject=CN=localhost, issuer=CN=localhost`, self-signed) so probing it by IP fails on BOTH issuer and hostname. Certificate validation was NOT disabled as a blanket default (that would be a security regression). Instead: `IHttpStatusProbe` gained a `GetAsync(url, allowUntrustedCertificate, ct)` overload whose DEFAULT INTERFACE IMPLEMENTATION IGNORES the flag (so every existing caller and test double keeps full validation); `HttpStatusProbe` honours it through a SEPARATE lazily-built HttpClient so relaxed TLS cannot leak into an ordinary probe; and `EndpointResolver` grants it ONLY when the user both configured the URL themselves AND ticked the per-endpoint 'Trust a self-signed certificate' switch (off by default, new `TrSetupSettings.TrustedSelfSignedEndpoints`). A profile's built-in default URL is ALWAYS fully validated — a stale opt-in can never weaken a built-in probe. Also fixed a latent bug found on the way: `JsonSettingsStore.LoadAsync` now re-applies `StringComparer.OrdinalIgnoreCase` to `Endpoints`/`AppRepoPaths`/`TrustedSelfSignedEndpoints`, because System.Text.Json rebuilds settable collections with the DEFAULT ordinal comparer — a casing-differing key worked in the session that saved it and silently missed after restart. Files: src/TrSetup.Core/Settings/EndpointResolver.cs (new), Settings/TrSetupSettings.cs, Settings/JsonSettingsStore.cs, Catalog/Probing/IHttpStatusProbe.cs, Catalog/Probing/HttpStatusProbe.cs, Profiles/Handlers/EndpointRequirementHandler.cs, Profiles/ProfileRequirementParamRules.cs, Profiles/BuiltIn/appstudio.json, src/TrSetupUI/Pages/Settings.razor, src/TrSetupUI/Services/BoardState.cs. Tests: +12 (new EndpointOverrideTests ×11 — default fallback, override wins, no-key ignores settings, trust granted only for an opted-in override, trust REFUSED for the profile default, Pass against the LAN endpoint with localhost never probed, unreachable evidence names endpoint+provenance, certificate rejection offers the trust opt-in, the relaxed-TLS flag reaches the probe only when opted in, per-detect re-resolution, built-in profile declares the key; +1 SettingsStoreTests round-trip incl. case-insensitivity) — full suite **169/169 green** (`dotnet test TrSetup.sln`, run 4×, stable). NOTE on the count: the '~149' baseline was a stale test assembly — a forced rebuild discovered 157 pre-existing + 12 new = 169. SMOKE (mandatory, this Mac, TrSetup.Web :5999, AppStudio, roles DeviceHostMac+AppRunnerMac+NativeDev; new spec tests/verify/appmanager-endpoint.spec.ts, PASS): drove the override through the real Settings UI (non-URL value → error + Save gated; then `https://192.168.1.14:5101/health` + trust switch on → Save), reopened /settings on a fresh circuit and BOTH the URL and the trust opt-in persisted (round-trip through ~/.trsetup/settings.json confirmed on disk), MacIp untouched. **`appstudio.appmanager-api` WENT GREEN**: row renders `Pass` with evidence `Endpoint https://192.168.1.14:5101/health [configured in Settings → Endpoints ['AppManagerUrl'], self-signed certificate explicitly trusted] answered 200.` (screenshots INSPECTED: test-results/appmanager-board-desktop.png, appmanager-board-mobile.png, appmanager-settings-desktop.png, appmanager-gate-desktop.png — data renders and the screen looks right, no overlap/clip/off-viewport, desktop body overflow 0). `appstudio.appmanager-secret` correctly STAYS RED ('Secret env var APPMANAGER_API_KEY is not set') — owner-supplied by design, ADR-008, NOT faked. **The Catalyst fixer's disabled-reason correctly NARROWED** to the one remaining prerequisite: 'Prerequisites still red — fix them first: appstudio.appmanager-secret.' (independently re-confirmed by the pre-existing phase-mac-ui.spec.ts REQ-FN-028 sheet test). Playwright: the 16 pre-existing + new specs all PASS (one unrelated failure in tests/verify/reqfn016-appium.spec.ts, a REQ-FN-016 spec created by a concurrent session at 11:52, not touched here). Endpoint-path note: App Manager answers 404 at `/` and 200 at `/health`; the profile default was left at `/` (unchanged, conservative — a 404 is Warn, not red, so it does not gate the fixer) and the UI hint tells the user to point the override at a 2xx URL. NOT Verified (ceiling is Implemented): the live green-path Catalyst build+install still cannot run here — it needs APPMANAGER_API_KEY exported by the owner; that remains owner UAT. ← 2026-07-20 [REQ-FN-028] VERIFIER PARTIAL (verify-phase phase-mac, live Mac). Acceptance half 1 — "fixer disabled with reason while any prerequisite red" — RUNTIME-CONFIRMED verbatim: the appstudio.maccatalyst-build row and its deep-linked detail sheet both render "Prerequisites still red — fix them first: appstudio.appmanager-api, appstudio.appmanager-secret. The Catalyst build fixer stays disabled until every prerequisite is green." (spec REQ-FN-028 PASS; sheet screenshot test-results/phase-mac-sheet-desktop.png). Acceptance half 2 — "success verified by the .app existing" — NOT OBSERVABLE this run: the two red prerequisites are owner-supplied by design (App Manager service on :5101 + APPMANAGER_API_KEY, ADR-008 — TrSetup never stores secrets), so the green path cannot be driven by the verifier. Stays Implemented pending owner green-path UAT. ‖ PRIOR: 2026-07-20 [REQ-FN-028] FIX (flow-master, Mac, clean machine) — defect (2) working dir resolves to process cwd: CLOSED. Root cause = `ProfilePaths.RepoRoot => RepoRootOverride ?? Directory.GetCurrentDirectory()`, which `MacCatalystBuildCheck` used as its build working dir; a published app launched from its own output folder therefore emitted `cd <publishFolder> && dotnet build …` for a directory containing no sources. Fixed by CONFIGURATION + VALIDATION, not a smarter fallback: new `TrSetupSettings.AppRepoPaths` (app → repo root) + `ProfilePaths.ResolveAppRepoRoot` which validates the configured path exists AND carries a repo/build marker (.git / *.sln / *.slnx / *.csproj); `MacCatalystBuildCheck` now takes a live settings accessor and REFUSES (running nothing) when the path is unconfigured, absent, or not a repo — it deliberately no longer falls back to cwd. FixPreview always still shows the literal `dotnet build -f net10.0-maccatalyst -c Release` but states `BLOCKED: <reason>` when unresolved. Files: src/TrSetup.Core/Settings/TrSetupSettings.cs, src/TrSetup.Core/Profiles/ProfilePaths.cs, src/TrSetup.Core/Catalog/Mac/MacCatalystBuildCheck.cs, src/TrSetup.Core/Catalog/CheckCatalog.cs. Tests: +5 in MacCatalystBuildCheckTests (unconfigured → refuse + no `dotnet build` invoked; missing dir → refuse; non-repo dir → refuse; preview BLOCKED and free of cwd; preview targets the configured repo) — suite 141/141 green (was 136). Smoke (this Mac, published :5999, AppStudio, deep link /check/appstudio.maccatalyst-build): FixPreview now reads `cd /Users/MyCode/AppStudio && dotnet build -f net10.0-maccatalyst -c Release → /Users/MyCode/AppStudio/bin/Release/net10.0-maccatalyst/AppStudio.app` — previously the publish folder. Playwright verify suite 15/15 green. NOT Verified: no verify-phase run executed this session (no docs/.last-verify.json); live green-path build+install remains owner UAT (needs green prereqs — appstudio.maui-workload/xcode/appmanager-api/appmanager-secret still red on this box). ← 2026-07-11 [REQ-FN-028] RE-VERIFIED verifiable portion (executed verify-phase, fix-issues re-verify, Mac, ledger docs/.last-verify.json): gate-detect budget fix CONFIRMED live — spec test hardened: deep link /check/appstudio.maccatalyst-build must SETTLE to real gate output (placeholder "not yet detected/never detected" must be streamed away) and the evidence must match red-prereq/ready/built — NEVER "Probe timed out": PASS, evidence settled to "Prerequisites still red — fix them first: appstudio.xcode, appstudio.appmanager-api, appstudio.appmanager-secret" (github-packages-feed correctly absent — Warn ≠ red, no longer hangs the gate); row settles on every load (see REQ-UI-001 full-settlement test); sheet renders title/Explain/FixPreview with the literal `dotnet build -f net10.0-maccatalyst -c Release` (§4a+§4b pass, test-results/phase-mac-sheet-desktop.png inspected); gate unit tests 10/10. NOT Verified because: defect (2) still open — FixPreview working dir resolves to the process cwd (observed live: src/TrSetup.Web → `…/TrSetup.Web/bin/Release/net10.0-maccatalyst/AppStudio.app`), no configured/validated AppStudio repo path; live green-path build+install remains owner UAT (needs full Xcode + green prereqs). ← 2026-07-11 [REQ-FN-028] FIX (builder agent, Mac) — defect (1) unbounded sequential gate detect: root cause = `CheckCatalog.DetectRedIdsAsync` re-ran all 8 AppRunnerMac prereq detects SEQUENTIALLY with no per-prereq bound, so one hanging prereq (github-packages-feed with no PAT) stalled the appstudio.maccatalyst-build row unboundedly (stuck ≥90 s). Fixed: prereq detects now run IN PARALLEL, each hard-bounded via `.WaitAsync` by the named constant `CheckCatalog.PrerequisiteProbeTimeout` (3.5 s < the engine's 5 s row budget); a prereq that times out or throws counts as RED (gate never assumes green) and reports honest evidence — `<id> (not confirmed green: timed out after 3.5 s)` / `(not confirmed green: probe threw <Type>)`; red-id list stays deterministic in catalog order (Task.WhenAll order). Files: src/TrSetup.Core/Catalog/CheckCatalog.cs (+ InternalsVisibleTo in TrSetup.Core.csproj for the gate tests). Tests: tests/unit/TrSetup.Core.Tests Catalog/CheckCatalogGateDetectTests (hanging prereq → returns within the bound, red with the "not confirmed green: timed out" suffix; throwing prereq → red with "probe threw" suffix; fast Pass/Fail mix → exactly the Fail ids; catalog order kept regardless of completion order; all-green → empty; constant < engine row budget) — full suite 10/10 green. Smoke (this Mac, :5999, AppStudio): appstudio.maccatalyst-build settled on ALL 6 fresh loads (was: never settled) as Fail with real gate evidence "Prerequisites still red — fix them first: appstudio.xcode, appstudio.appmanager-api, appstudio.appmanager-secret" (github-packages-feed correctly NOT listed — it detects Warn, not Fail, and no longer hangs the gate). Defect (2) FixPreview working dir resolving to process cwd (no configured AppStudio repo path) is OUT OF SCOPE of this fix and remains a known issue. ← 2026-07-11 [REQ-FN-028] verify-phase phase-mac (Mac, first live run on macOS): row + detail sheet render (deep link /check/appstudio.maccatalyst-build → title/Explain/FixPreview with the literal `dotnet build -f net10.0-maccatalyst -c Release` command — spec test PASS). TWO DEFECTS FOUND: (1) ⚠ its DetectAsync re-runs all 8 AppRunnerMac prereq detects SEQUENTIALLY (CheckCatalog.DetectRedIdsAsync) outside the engine's 5s budget and never settled on this Mac (row stuck "Pending" ≥90s every load, evidence pane "never detected") — suspect the github-packages-feed prereq blocks with no PAT; (2) FixPreview working dir resolves to the process cwd (the publish folder), not an AppStudio repo — no repo path is configured/validated. Live green-path build remains owner UAT (needs full Xcode + green prereqs). ← 2026-07-07 [REQ-FN-028] MacCatalystBuildCheck: non-macOS→NotApplicable; disabled-while-red gate via live red-prereq-ids delegate (any AppRunnerMac profile check Fail → Fail naming ids; FixAsync refuses without running build unless red set empty); all-green→Pass if .app exists; FixPreview shows literal `dotnet build -f net10.0-maccatalyst -c Release` + .app path. MacCatalystBuildCheckTests (gating, preview, off-mac NA) + E2E (NotApplicable off-macOS). Live Catalyst build = Mac UAT (not run). | [view](#d-req-fn-028) |
| REQ-FN-029 | Disk-space check with configurable floor (P3) | Verified | 100 | 2026-07-07 [REQ-FN-029] DiskSpaceCheck/DiskSpaceRequirementHandler: reads free space on configured `path` (default managed root) via DriveInfo vs `floorGb`, breach→Warn (NEVER Fail) with free-vs-required GB in evidence, no fixer (guidance only). Unit-tested (breach→Warn w/ figures, above-floor→Pass). E2E render-truth: "936.9 GB free on '~/.trsetup' (floor 80 GB)" Pass. | [view](#d-req-fn-029) |
| REQ-FN-030 | MAUI Blazor Hybrid head — Win unpackaged + Mac Catalyst (P4) | Implemented | 94 | 2026-07-21 ⚠ **RETRACTION — the "Release build ships an empty `wwwroot`" defect WAS NOT REAL.** Recorded twice today (once "confirmed on a clean build") and wrong both times. There is **no build defect**: `dotnet build -c Release` for Catalyst defaults to `RuntimeIdentifiers=maccatalyst-x64;maccatalyst-arm64`, so it runs two per-RID **inner** builds and lipo-merges them into ONE **universal** bundle at `bin/Release/net10.0-maccatalyst/TrSetup.app` — **with no RID sub-folder**. That bundle's `wwwroot` is complete (`_content`, `_framework`, `app.css`, `index.html`, `lib`; binary `x86_64 arm64`). The `maccatalyst-arm64/` and `maccatalyst-x64/` sub-folders everyone kept inspecting are **intermediates whose empty wwwroot is BY DESIGN** — MAUI's `DisableResizetizer` gate (`Microsoft.Maui.Resizetizer.After.targets`, keyed on `_IsMultiRidBuild`) deliberately skips asset staging for inner builds since only the outer build stages assets. Debug builds a single RID, so there `maccatalyst-arm64/TrSetup.app` genuinely IS the deliverable — that asymmetry is exactly what made this masquerade as a Release-only regression. Proven decisively: `dotnet build -c Release -p:RuntimeIdentifier=maccatalyst-arm64` (single RID ⇒ `_IsMultiRidBuild` false) DOES populate the arm64 folder — same configuration, only the RID count changed. Item-level measurement confirms the pipeline was never at fault: `@(StaticWebAsset)` 91 and `@(MauiAsset)` 89 are **byte-identical** across outer and both inner builds; only the downstream bundle-resource staging differs (88 vs 0). The earlier "MauiAsset has only index.html in Release" lead was re-measured and **does not hold**. ROOT CAUSE OF THE WHOLE SAGA = **a wrong path in our own docs**: `TrSetup-BuildAndRun-Guide.md` §3 Option B pointed at the RID sub-folder and claimed "the RID sub-folder follows the build host's architecture". Both statements were wrong and are now FIXED, with the trap documented so nobody re-investigates it a third time. ✅ **CONSEQUENCE — Release-head drivability is now PROVEN** (it was open since 2026-07-21 morning): the universal Release `.app` was launched and driven over Appium mac2 — **412 nodes total, 209 in the `TrSetupBlazorWebView` subtree**, zero occurrences of "There is no content at", live board text read out of it (`Environment board`, `Node.js v22.23.1 present`, `'dotnet' present: 10.0.302`, Pass/Fail/Warn badges). The old "Release head returned 'application under test is not running'" symptom was the same wrong-artifact mistake. ✅ **The Debug-only automation gate is now verified NON-VACUOUSLY for the first time** — previously it could only be "confirmed" against a bundle that contained nothing at all, which proved nothing. Against the real universal Release bundle, `find` for `*automation*` / `index.debug.html` returns NOTHING, while the Debug bundle carries both. The gate discriminates correctly and needed no change. ‖ PRIOR: 2026-07-21 **NATIVE TESTABILITY ADDRESSED (fix-issues) — and an important NEGATIVE finding recorded.** The AutomationId gap logged earlier today was attacked empirically against the live head: five candidate HTML attributes were stacked on a single control proven reachable, and **NO attribute can populate XCUITest `identifier` for WKWebView content** — `id`, `name`, `title`, `aria-describedby`, `aria-roledescription` all surfaced nothing; `role` only changes `elementType`; `aria-label` surfaces as `label` (the accessible NAME). The negatives are trustworthy because `aria-label` was then set on that same element and did change `label`, proving the element was reachable. **So the literal acceptance clause "AutomationIds present" is architecturally unachievable for Blazor-in-WebView** — `identifier` remains 1 of 152 (the MAUI host control) and always will. The clause's INTENT (stable, text-independent native locators) was met another way: the only channel found to reach XCUITest is `aria-placeholder` → `value`, so `src/TrSetupUI/wwwroot/js/automation-ids.js` mirrors every `data-testid` onto it via a MutationObserver (`data-testid` stays the single source of truth; zero `.razor` files and zero testids changed). `aria-label` was deliberately NOT used — overwriting the accessible name is exactly the pollution the REQ-NFR-003 icon bug caused. RESULT ON THE LIVE NATIVE HEAD: **0 → 48 automation-id-bearing controls** (`NavBoard`, `NavSettings`, `RecheckAllButton`, `ExportReportButton`, `fix-all-framework-core`, `preview-mac.appium-launchagent`, `row-title-…`), locatable as `-ios predicate string: value == 'RecheckAllButton'`, with every accessible label byte-identical before and after. ⚠ **OWNER DECISION 2026-07-21 — GATED TO DEBUG.** `aria-placeholder` on a button/link is an ARIA-spec violation (meaningful only on textbox/searchbox/combobox/spinbutton) with an untested risk that VoiceOver announces it, so the mirror ships in **Debug only**: the MAUI head loads `wwwroot/index.debug.html` under `#if DEBUG` (`MainPage.xaml.cs`), and `TrSetup.csproj` strips both that document and the script (incl. `.br`/`.gz`) from Release `MauiAsset`. Verified both ways at bundle level — Debug `.app` carries `index.debug.html` + `_content/TrSetupUI/js/automation-ids.js`; Release `.app` contains zero hits for either. Automation loses nothing: the registered verify recipe drives the Debug head. `TrSetup.Web` (test-only host, never distributed) stays un-gated. **STILL NOT Verified** — the **Windows half** (rung #4 build + boot) remains unobservable on macOS. ⚠ ALSO CONFIRMED THIS RUN: the Release-empty-`wwwroot` defect is **real and reproducible on a CLEAN build** — the content previously seen there was stale (timestamps predating the rebuild); after clearing and rebuilding, `Contents/Resources/wwwroot` is EMPTY. This upgrades the previously "reported by a sub-agent, not confirmed" open item to confirmed. ‖ PRIOR: 2026-07-21 [REQ-FN-030] **VERIFIER RUN — first time the §4a render gate and §4b visual-truth gate have EVER run against the real Catalyst head rather than the web-host proxy.** `⚠ STATIC-ONLY` is now lifted by the verifier itself (the prior lift was a self-run determination). Method per the registered recipe: `open -a` the Debug `.app`, then a SYSTEM-SCOPED mac2 session with **no** `appium:bundleId`, then `macos: activateApp` — reproduced cleanly. **§4a RENDER GATE — PASS.** The XCUITest tree is 415 nodes / 104 KB, of which the `XCUIElementTypeWebView identifier="TrSetupBlazorWebView"` subtree holds 155 nodes and **100 text-bearing Blazor controls** (StaticText/Button/Link) carrying real, evidence-bearing data read straight out of the running native head — board groups ("Framework core", counts 11 ✓ / 1 ⚠ / 3 ✗), per-row verdicts and evidence ("Appium 3.5.2 with xcuitest@11.17.7, mac2@4.0.4 …", "Full Xcode active at /Applications/Xcode.app/Contents/Developer", "Node.js v22.23.1 present", "Current LAN address 192.168.64.2 does NOT match the configured endpoint 192.168.1.77"), and the correct per-row action affordances (Preview/Fix on fixable rows, Open guide on manual-only rows). No control rendered empty. **§4b VISUAL-TRUTH GATE — PASS.** Every text-bearing control has a non-degenerate rect (**0 zero-size controls**), no overlapping sibling rects, nothing off-window; full screenshot `test-results/phase-mac-NATIVE-catalyst.png` inspected — the board is fully styled (native title bar + traffic lights, sidebar rail, grouped cards, status pills), no stacking, clipping or unstyled fallback. **⚠ NEW DEFECT — AutomationIds do NOT survive into the WebView, which is this REQ's own acceptance clause.** Acceptance requires "AutomationIds present (REQ-NFR-005)"; of the 155 nodes in the WebView subtree **exactly ONE carries an `identifier` — `TrSetupBlazorWebView` itself (the MAUI host control)**. Every Blazor control inside (Board / Fix all run / Report / Setup / Settings links, Fix all, Preview, Fix, Re-check buttons) exposes `identifier=""`. Cause: the UI is instrumented with `data-testid` (correct and sufficient for Playwright — `MainLayout.razor:26-50` etc.), but XCUITest surfaces only `aria-label`/`id`/`title`, never `data-testid`, so **native automation of this app is reachable only by fragile label-text matching** — and that fragility is already demonstrated, since the one nav item whose label is polluted by the NFR-003 icon bug now matches as `"Icon not found: sliders Settings"`. Per verify-phase §3b a missing AutomationId is a defect, not a license. FIX = emit `id` or `aria-label` alongside `data-testid` on interactive controls (one shared helper), then re-verify by asserting identifiers in the mac2 tree. **NOT promoted to Verified** — two acceptance clauses remain unmet: the AutomationId clause above, and the **Windows half** (rung #4 build + boot), which stays unobservable on macOS (csproj TFM guard excludes the Windows TFM here, and the Windows host at 192.168.1.14 exposes no remote-exec channel). Release-head drivability likewise still unproven — this run drove the Debug head, as the registered recipe intends. ‖ PRIOR: 2026-07-21 [REQ-FN-030] ⚠ STATIC-ONLY LIFTED — the Catalyst head is now **FULLY DRIVABLE** (outcome 1), and the old "XCUITest cannot see Blazor Hybrid WebView content" finding is **REFUTED**; separately a **NEW ship-blocking Release-build defect** was found. Re-tested against the Appium stack repaired under REQ-FN-016 (server 3.5.2 + mac2@4.0.4 + xcuitest@11.17.7, pinned `APPIUM_HOME=/Users/sravikumar/.trsetup/tools/appium`, Node v22.23.1) — the prior 4 blocked approaches were all recorded against the broken/mismatched Appium 2.0.1. **(1) mac2 ATTACHES and the accessibility tree DOES descend into the BlazorWebView.** A system-scoped mac2 session against the running head returned a 104 KB tree containing `XCUIElementTypeWebView identifier="TrSetupBlazorWebView"` and, INSIDE it, **101 real Blazor DOM nodes** — 48 StaticText / 47 Button / 8 Link / 18 Image / 1 PopUpButton. The exact strings the old row said were "all absent" are present: 'Environment board' ✓, 'Fix all' ✓, 'Node.js' ✓ — plus live per-row detect evidence read straight out of the DOM ("Appium 3.5.2 with xcuitest@11.17.7, mac2@4.0.4 in APPIUM_HOME=…", "Full Xcode active at /Applications/Xcode.app/Contents/Developer", "Current LAN address 192.168.64.2 does NOT match the configured endpoint 192.168.1.77"). **(2) Element-based ACTION works, not just reading** — `findElement(-ios predicate string, label == "Toggle Sidebar")` on a Blazor `<button>` followed by `POST …/element/{id}/click` returned 200 and the rendered UI CHANGED (sidebar collapsed to an icon rail; before/after window screenshots both captured, then clicked again to restore). No coordinate math involved, so the old approach-(3) Finder-scoped-click and approach-(4) `osascript` dead ends are moot. **ROOT CAUSE of the old false finding:** the tree was never opaque — the app under test was rendering a blank 404 page, so there was simply no board content in the DOM to see. The old approach-(2) observation ("Appium launches the app → blank window reading 'There is no content at'") was misattributed to the XCUITest launch; it is really the Release-build asset defect below. **(3) ⚠ NEW DEFECT — `dotnet build -c Release` ships a Catalyst .app that CANNOT RENDER.** The Release bundle produced by the command documented in BuildAndRun-Guide §3 Option B contains **NO `Contents/Resources/wwwroot`** (`find TrSetup.app -name index.html` → nothing), so the head boots to "There is no content at " — confirmed live, both by screenshot and by that exact string being the ONLY text node in the WebView subtree. The Debug bundle DOES carry `Contents/Resources/wwwroot/index.html`, and `dotnet publish -f net10.0-maccatalyst -c Release -p:RuntimeIdentifier=maccatalyst-arm64` also produces it and renders the full board correctly. Static web assets ARE computed in Release (`obj/Release/…/staticwebassets.build.json` exists) — they are just never copied into the bundle by the build target. **So the documented build command yields a broken app; publish is currently required.** Guide/csproj fix NOT attempted here (out of scope for a determination task) — logged as the top follow-up. **(4) Appium block REGISTERED** in `.tfcore/core-config.yaml` under `runtimeVerification.appium.maccatalyst` (url http://127.0.0.1:4725, automationName mac2), with the working recipe recorded inline: **launch the app FIRST via `open -a …TrSetup.app`, THEN create a SYSTEM-SCOPED session (NO `appium:bundleId` capability), then `macos: activateApp`.** Passing `bundleId` as a capability makes Appium kill+relaunch the app (PID observed changing 15273→15341), which re-triggers the blank-content path; and killing the AUT always kills the WDA session, so order matters. EVIDENCE — real screenshots of the RUNNING NATIVE HEAD (window-scoped `element/{id}/screenshot`, NOT the TrSetup.Web smoke host): `test-results/reqfn030-maccatalyst-head.png` (full styled board, native traffic-lights + "TrSetup" title bar, 11 Pass / 1 Warn / 3 Fail with real evidence) and `test-results/reqfn030-maccatalyst-head-after-click.png` (post-click, sidebar collapsed). Whole chain re-run clean end-to-end at the finish to prove it reproduces. ⚠ ALSO HIT: the pre-existing entitlements/stale-AOT gotcha reproduced exactly as documented below (incremental Release build SIGABRTed in `load_aot_module`; clearing `bin`/`obj` and rebuilding fixed it) — that gotcha is real and still load-bearing. **HONEST LIMITS — what is NOT claimed:** (a) attaching to the *Release-published* head was FLAKY in this session — `macos: activateApp` returned null but WDA then reported "The application under test is not running, possibly crashed" across several retries, while the Debug head attached reliably every time; the drivability finding above rests on the Debug head, so the Release artifact's drivability is NOT established. (b) End-to-end "click Fix in the MAUI head → install lands on the real system" STILL not demonstrated — no Fix was clicked (fixes install software; out of scope for a read-only determination). (c) The Windows half (rung #4 build + boot) remains unobservable on macOS — csproj TFM guard excludes the Windows TFM here. (d) Ceiling is **Implemented**, not Verified — this was a self-run determination, not a verify-phase run. OWNER UAT: click a real Fix from the native head and confirm the install lands system-wide; and the Windows unpackaged head on a Windows box. ‖ PRIOR: 2026-07-20 [REQ-FN-030] VERIFIER — MAUI head ⚠ STATIC-ONLY + two build-level confirmations (verify-phase phase-mac). (a) Mac Catalyst head BUILDS clean this run: TrSetup.dll → net10.0-maccatalyst/maccatalyst-arm64, solution 0 errors / 6 warnings (all the known AngleSharp GHSA-pgww-w46g-26qg advisory). (b) **App Sandbox fix confirmed in the signed artifact, not just source**: `codesign -d --entitlements` on the built TrSetup.app reports `com.apple.security.app-sandbox = false` — this is the binary-level proof the 2026-07-20 sandbox defect is closed. (c) The RUNNING head is **⚠ STATIC-ONLY**: no `runtimeVerification.appium` block is registered in .tfcore/core-config.yaml and FN-030 already documents XCUITest as unable to expose Blazor Hybrid WebView content (4 approaches, all blocked), so no agent can drive the head — the §4a/§4b gates for this REQ ran against the TrSetup.Web smoke host hosting the same TrSetupUI RCL, NOT the Catalyst head. Windows half (rung #4 build + boot) not observable on macOS — the csproj TFM guard excludes the Windows TFM here. Both remain owner UAT; nothing here is claimed as a visual pass on the native head. ‖ PRIOR: 2026-07-20 [REQ-FN-030] MAUI-HEAD AUTOMATION ASSESSED (owner granted macOS Accessibility + Screen Recording). **Accessibility grant WORKS** — WebDriverAgentMac now builds AND runs ('WebDriverAgent is ready to accept commands'), the earlier 'Timed out while enabling automation mode' is gone, `screencapture` works, and both a system-scoped and an app-scoped (bundleId com.techierathore.trsetup) Appium/mac2 session were created successfully. Screenshot evidence captured: the MAUI head renders the full styled board natively (sidebar, cards, real detect evidence) and reports Full Xcode / .NET SDK + MAUI workload / Node.js all Pass — the un-sandboxed build is healthy. **BUT automated UI driving of this head is NOT achievable with this stack — 4 approaches tried, all blocked:** (1) XCUITest does NOT expose Blazor Hybrid WKWebView content — the app node is present but the accessibility tree contains NO board rows, buttons or text ('Environment board', 'Fix all', 'Node.js' all absent), so element-based driving is impossible by architecture, not configuration. (2) An app-scoped session where APPIUM launches the app renders a blank window reading **'There is no content at'** — the Blazor content host fails under an XCUITest launch, while the same build launched normally via `open` renders perfectly (⚠ genuine fragility, logged as a follow-up). (3) A system-scoped session's coordinate clicks are Finder-scoped, not screen-scoped — a click at the TrSetup Fix button opened a Finder window instead. (4) `osascript` System Events `click at` hangs indefinitely. Screen geometry was confirmed 2240x1260 logical via WDA, so the coordinate math was not the problem. **Consequence for the verification strategy: the MAUI head cannot be gated by an agent — its rows are owner-manual UAT.** The shared TrSetupUI RCL stays agent-verifiable through the web host, subject to the muxer/PATH caveat recorded under REQ-FN-016. NOT Verified: end-to-end 'click Fix in the MAUI head => install lands on the real system' is STILL unproven and is now owner UAT. ← 2026-07-20 [REQ-FN-030] ⚠ ARCHITECTURAL DEFECT FOUND + FIXED (flow-master, Mac, owner challenge "I am still not able to install something which is not on system using this tool"). **The shipped Mac Catalyst head was App-Sandboxed, which structurally prevents TrSetup from doing the one thing it exists to do.** `Platforms/MacCatalyst/Entitlements.plist` carried `com.apple.security.app-sandbox = true` (present only because it is required for Mac App Store distribution — TrSetup is a directly-distributed developer tool and was never App Store bound). Under the sandbox macOS silently redirects the app's filesystem writes into `~/Library/Containers/com.techierathore.trsetup/Data/` and blocks execution of downloaded binaries, so EVERY fixer appeared to do nothing. Hard evidence captured live before the fix: (a) the app kept its own private `~/Library/Containers/com.techierathore.trsetup/Data/.trsetup/settings.json` (Endpoints {}) completely divorced from the real `~/.trsetup/settings.json` (which carried MacIp + AppRepoPaths) — the two heads never shared config; (b) an owner Fix click on the .NET SDK row had downloaded `…/Data/.trsetup/tools/dotnet-install/dotnet-install.sh` INTO THE CONTAINER, where it could never install anything system-wide; (c) `~/.zprofile` is not reachable from the container, so the `mac.node-path` managed PATH block could never be written by the MAUI head. Fixed: `app-sandbox` set to `false` with a comment recording why it must stay false (`network.client` retained for installer downloads / endpoint probes). Verified: rebuilt `.app` signs with `com.apple.security.app-sandbox => false`; relaunched app creates NO container `.trsetup` — writes are no longer redirected. ⚠ GOTCHA for future changes: an INCREMENTAL rebuild after editing entitlements produces a bundle that SIGABRTs at startup in `load_aot_module` (stale AOT images beside a fresh signature) — `rm -rf src/TrSetup/bin src/TrSetup/obj` and rebuild clean, which restored a healthy launch. NOT Verified: end-to-end "click Fix in the MAUI head → tool lands on the real system" is NOT yet demonstrated — the Appium mac2 harness was stood up this session (appium 3.5.2 + mac2 4.0.4, WebDriverAgentMac builds under "Sign to Run Locally") but the session dies with "Timed out while enabling automation mode", i.e. macOS Accessibility (TCC) must be granted to the runner by the owner. Owner UAT: grant Accessibility + Screen Recording, then run a Fix from the MAUI head. ← 2026-07-11 [REQ-FN-030] NETSDK1100 FAIL CLEARED (executed verify-phase, fix-issues re-verify, Mac, ledger docs/.last-verify.json): the verifier re-ran the documented command VERBATIM from the repo root after the csproj fix (`dotnet build src/TrSetup -f net10.0-maccatalyst -c Release`) → 0×NETSDK1100 in .verify/catalyst-build4.log; restore + TrSetup.Core/TrSetupUI compile clean and the build stops ONLY at `Could not find a valid Xcode app bundle at '/Library/Developer/CommandLineTools'` — legitimate host state on this CLT-only Mac, matching the board's own mac.xcode Fail row (real evidence observed live on the board). Status Implemented (not Verified): producing/booting the Catalyst .app needs a Mac with full Xcode (owner UAT — install Xcode or `xcode-select -s /Applications/Xcode.app`, then re-run *verify REQ-FN-030); Windows clauses remain verified per the 2026-07-10 run. ← 2026-07-11 [REQ-FN-030] FIX (builder, on the Mac): root cause — the OSX guard on `<TargetFrameworks>` only APPENDED `net10.0-maccatalyst`, leaving the windows TFM first in the list, so restore on macOS still evaluated `net10.0-windows10.0.19041.0` → NETSDK1100. Fix — the guard now REPLACES the list with `net10.0-maccatalyst` on OSX (`src/TrSetup/TrSetup.csproj`, comment updated to describe the both-ways guard); on Windows the condition is false so the windows-only list is unchanged (reasoned statically — the Windows head cannot be built on this Mac). Smoke: `dotnet build src/TrSetup --getProperty:TargetFrameworks` on this Mac → `net10.0-maccatalyst` only; the documented command run VERBATIM → restore clean (no NETSDK1100, no EnableWindowsTargeting flag), TrSetup.Core + TrSetupUI compiled, fails ONLY on the missing-full-Xcode error (CLT-only host, same class as catalyst-build2.log) — evidence .verify/catalyst-build3.log. ← 2026-07-11 [REQ-FN-030] FAIL on the Mac Catalyst clause (verify-phase phase-mac, FIRST real-Mac attempt): the documented build command `dotnet build src/TrSetup -f net10.0-maccatalyst -c Release` FAILS on macOS with NETSDK1100 — restore evaluates the net10.0-windows TFM (unconditionally first in `<TargetFrameworks>`) and Windows targeting is not enabled on macOS; the OSX guard only APPENDS maccatalyst, it never excludes the windows TFM (.verify/catalyst-build.log). Retry with `-p:EnableWindowsTargeting=true` gets past restore, then fails for full Xcode (CLT-only machine — legitimate host state, matches the board's own mac.xcode row; .verify/catalyst-build2.log). FIX = csproj (`EnableWindowsTargeting` on OSX, or OSX-conditional TFM list) or guide flag — until then NO Mac can build the Catalyst head as documented. Windows clauses remain verified per 2026-07-10 run. ← 2026-07-07 [REQ-FN-030] TrSetup.App shell: BlazorWebView `AutomationId=TrSetupBlazorWebView` + ContentPage `AutomationId=TrSetupMainPage` (NFR-005 MAUI half). csproj BRD-44: `WindowsPackageType=None` (unpackaged) ✓ pre-existing, OSX-guarded `net10.0-maccatalyst` TFM ✓ pre-existing; ADDED maccatalyst-guarded ad-hoc signing PropertyGroup (`CodesignKey=-`, `CreatePackage=false`). Rung-#4 Windows build PASS (0W/0E) → `bin/Release/net10.0-windows10.0.19041.0/win-x64/TrSetup.App.exe`; full `TrSetup.sln` Debug build PASS 0W/0E (7 proj incl. MAUI). Boot attempted from WSL: direct-run held alive full 25s (exit 124 timeout-kill, no crash) + live PID observed mid-run = process boots & persists; visual "renders the board" is host-bound UAT (headless WSL can't observe the Windows GUI desktop). Catalyst build documented in BuildAndRun-Guide §3 Option A (`dotnet build -f net10.0-maccatalyst -c Release` → `maccatalyst-arm64/TrSetup.app`). Grade reconciled (flow-master inline): Implemented — build+AutomationIds+boot-persist+Catalyst-doc done here; the visual "boots to the board" render + the Mac Catalyst build are the only host-bound clauses left (promotes to Verified on a Windows/Mac session). FIX 2026-07-07 (owner-reported PCA popup "This program might not have installed correctly"): the exe name `TrSetup.App.exe` contains "Setup" → Windows Program Compatibility Assistant installer-detection heuristic false-fires (surfaced when the P4 boot-attempt killed the exe abnormally). Fixed by embedding a proper Win32 manifest — added `<compatibility>` supportedOS (Win7→11) + `asInvoker <trustInfo>` to `Platforms/Windows/app.manifest` and referenced it via windows-scoped `<ApplicationManifest>` in the csproj (was unreferenced → not embedded before). Rung-#4 rebuild PASS 0W/0E; verified all four manifest markers (asInvoker/supportedOS/compatibility.v1/longPathAware) embedded in the exe. | [view](#d-req-fn-030) |
| REQ-FN-031 | BuildAndRun guide firm-up + distribution (P4) | Verified | 100 | VERIFIED 2026-07-11 (executed verify-phase, fix-issues re-verify, Mac, ledger docs/.last-verify.json): the ONLY remaining re-verify scope (§3 Option B Catalyst command) is closed — the verifier ran the guide's command VERBATIM on this Mac (.verify/catalyst-build4.log): it now behaves EXACTLY as the guide documents — clean restore (no NETSDK1100, no extra flag), stopping only at the full-Xcode prerequisite the guide now states explicitly, with the exact failure text and the `xcode-select -s /Applications/Xcode.app` remedy in §4 troubleshooting (guide .md + .html both updated and consistent). Guide accuracy across the rest re-confirmed this cycle: §2 Windows CLI verified 2026-07-10; §3 Option A / A′ Mac web-head boot re-exercised today (this verify ran on :5999 booted per the guide from the repo root). The .app-production end-state itself is tracked by REQ-FN-030 (Xcode-host UAT), not by this guide REQ. ← 2026-07-11 [REQ-FN-031] FIX (builder, guide edit after the REQ-FN-030 csproj fix): §3 Option B stale bullet corrected — the guard now REPLACES `<TargetFrameworks>` with maccatalyst on macOS, so the documented command restores/builds cleanly there (no NETSDK1100, no EnableWindowsTargeting flag); full-Xcode prerequisite made explicit with the exact CLT-only failure hint (`Could not find a valid Xcode app bundle at '/Library/Developer/CommandLineTools'` → the board's mac.xcode row must be green first) and a matching §4 troubleshooting bullet; same edits mirrored in docs/TrSetup-BuildAndRun-Guide.html. Smoke: the guide's §3 Option B command run VERBATIM on this CLT-only Mac → restore clean, fails only on the now-documented missing-full-Xcode error — .verify/catalyst-build3.log. ← 2026-07-11 [REQ-FN-031] DEMOTED (verify-phase phase-mac): the guide's §3 Option B Catalyst command was run VERBATIM on a real Mac for the first time and FAILS (NETSDK1100 — see REQ-FN-030 remark + .verify/catalyst-build.log); the "NETSDK1139 on Windows is expected" note masked that the command was never exercised on macOS either. The §3 A′ boot clause is now FIXED IN CODE (content root pinned, REQ-FN-034 FIX #2 — the documented repo-root launch re-verified styled); remaining re-verify scope is ONLY the §3 Option B Catalyst command (NETSDK1100). §2 Windows path unaffected. ← VERIFIED 2026-07-10 (executed verify-phase, run ledger docs/.last-verify.json): after the REQ-FN-035 rename the guide's §2 CLI command was re-run VERBATIM at its new path (`cmd.exe /c "dotnet build src/TrSetup -f net10.0-windows10.0.19041.0 -c Release"`, ladder rung #4) → 0W/0E → runnable `src/TrSetup/bin/Release/net10.0-windows10.0.19041.0/win-x64/TrSetup.exe`, which was then BOOTED (process persisted, Serilog log written). §3 Option A (Web osx-arm64 publish) unchanged; §3 Option B Catalyst command stays documented Mac-host UAT (`NETSDK1139` on Windows documented + expected). Guide paths updated to the renamed head. HISTORY — DEMOTED 2026-07-10 earlier that day: the 2026-07-09 "RE-VERIFIED" below was SELF-ATTESTED by the build orchestrator — verify-phase was never executed (agent-confirmed). 2026-07-09 [REQ-FN-031] RE-VERIFIED after the MAUI-only rewrite: ran the guide's §2 Windows CLI command verbatim (`cmd.exe /c "dotnet build src\TrSetup.App -f net10.0-windows10.0.19041.0 -c Release"`, = ladder rung #4) → 0W/0E → produced the runnable `src/TrSetup.App/bin/Release/net10.0-windows10.0.19041.0/win-x64/TrSetup.App.exe`. The §3 Mac Catalyst command (`dotnet build … -f net10.0-maccatalyst`) stays Mac-host UAT (the TFM only activates on macOS — `NETSDK1139` on Windows is documented + expected). Withdrawn Cli/Web publish scripts removed by REQ-FN-034. | [view](#d-req-fn-031) |
| REQ-NFR-006 | Portability — self-contained Cli/Web publishes (P4) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: single MAUI desktop app — the Cli/Web bootstrap path is retired with its heads). Was Verified 2026-07-07. Script removal tracked by REQ-FN-034. | [view](#d-req-nfr-006) |
| REQ-FN-032 | `trsetup --check --json` agent mode (P5) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: single MAUI desktop app — agent mode lived in the CLI head). Was Verified 2026-07-07; docs/TrSetup-AgentMode.md archived to OldDocs. Removal tracked by REQ-FN-034. | [view](#d-req-fn-032) |
| REQ-FN-033 | Verifier pre-flight hook (P5, stretch) | N/A | — | WITHDRAWN 2026-07-09 (owner decision: consumed FN-032's CLI agent mode, which is withdrawn; was optional/stretch per BRD-47). Was Verified 2026-07-07. `scripts/preflight-gate.*` removal tracked by REQ-FN-034. | [view](#d-req-fn-033) |
| REQ-FN-034 | Decommission CLI + Web heads → single MAUI desktop app (P6) | Verified | 100 | 2026-07-11 [REQ-FN-034] FIX #2 (fix-issues, owner-reported issuessc/TrSetup.Web-Mac.png — board rendered raw unstyled HTML on the Mac): root cause = `WebApplication.CreateBuilder(args)` defaults ContentRoot to the LAUNCH cwd, so a publish started from any other directory (the UsageGuide §3 A′ runbook itself says run `./publish/mac/TrSetup.Web/TrSetup.Web` from repo root!) resolved wwwroot/ wrongly → every asset 404 (blazor.web.js + 3 css). Fixed: `ContentRootPath = AppContext.BaseDirectory` pinned in Program.cs. RE-VERIFIED same day (ledger docs/.last-verify.json): publish rebuilt on the Mac; booted from repo root, from publish/mac (the owner's exact failing cwd), and via `dotnet run` — all 4 assets 200 in all 3 modes; 7/7 Playwright specs incl. zero-console-errors; desktop screenshot inspected styled. ← 2026-07-11 [REQ-FN-034] Smoke-host hardening (agent, Mac UAT-blocker fix): `TrSetup.Web/Program.cs` `UseStaticWebAssets` call wrapped in try/catch(DirectoryNotFoundException) — previously a `bin/Debug` output copied to another machine crashed at startup because the static-asset manifest points at build-machine paths. Product MAUI head untouched; Web remains test-only. Mac osx-arm64 publish + boot + Playwright smoke PASS (styled render, 0 errors). Next WSL verify-phase run re-covers the :5999 boot. ← VERIFIED 2026-07-10 (executed verify-phase, run ledger docs/.last-verify.json): solution = 4 product projects (TrSetup [MAUI head, renamed by FN-035]/TrSetupUI/Core/Core.Tests) + Web smoke host, builds green rung #4 0W/0E (5 projects); `TrSetup.Core.Tests` 126/126 pass (rung #2, this run); repo grep = ZERO `TrSetup.Cli` refs in source/tests/harness (only docs' withdrawal-record mentions + OldDocs); `playwright.config.ts` boots `TrSetup.Web` on :5999 as the test-only smoke host — booted live this run and the full 7/7 verify suite ran against it. HISTORY — DEMOTED 2026-07-10 earlier that day: the "VERIFIED" below was SELF-ATTESTED by the build orchestrator — verify-phase was never executed (agent-confirmed). 2026-07-09 [REQ-FN-034] Deleted `src/TrSetup.Cli`, `tests/unit/TrSetup.Cli.Tests`, `scripts/publish.sh`/`publish.ps1`, `scripts/preflight-gate.sh`/`preflight-gate.ps1`; pruned `TrSetup.sln` (project entry + config-platform block + nested-project line for both Cli GUIDs). **OWNER-AMENDED end-state (2026-07-09 build):** owner chose "keep a headless smoke host", so `TrSetup.Web` is RETAINED — reclassified from a shipping head to the **test-only headless UI smoke host** (`playwright.config.ts` still boots it on :5999; it hosts the same `TrSetupUI` RCL) so UI smoke keeps running headlessly in WSL. Product shipping surface = the single MAUI `TrSetup.App`. VERIFIED: solution now = 4 product projects (App/TrSetupUI/Core/Core.Tests) + Web(smoke host); builds green rung #4 (0W/0E, 5 projects incl. MAUI); `TrSetup.Core.Tests` 126/126 pass; repo grep confirms zero `TrSetup.Cli` refs in source/tests/harness outside `docs/OldDocs/`. | [view](#d-req-fn-034) |
| REQ-NFR-007 | Observability — Serilog file-based logging in the MAUI head (P6) | Verified | 100 | VERIFIED 2026-07-10 (executed verify-phase, run ledger docs/.last-verify.json): BOOTED the renamed `TrSetup.exe` (rung #4) — today's log `…\User Name\com.techierathore.trsetup\Data\logs\trsetup-20260710.log` contains the startup line ("TrSetup starting (version 1.0.0.1, build 1)") AND a `TrSetup.Core.Settings.JsonSettingsStore` event, proving the shared-library `ILogger<T>`→Serilog pipeline live; grep confirms NO Serilog reference in `TrSetupUI.csproj`/`TrSetup.Core.csproj` (MEL abstractions only); `AppDomain.UnhandledException`/`TaskScheduler.UnobservedTaskException`→`Log.Fatal` + `Log.CloseAndFlush()` on `Window.Destroying` present in `src/TrSetup/MauiProgram.cs`/`App.xaml.cs`. HISTORY — DEMOTED 2026-07-10 earlier that day: the "VERIFIED" below was SELF-ATTESTED by the build orchestrator — verify-phase was never executed (agent-confirmed). 2026-07-09 [REQ-NFR-007] Wired Serilog in `TrSetup.App` (pkgs Serilog 4.2 / Serilog.Extensions.Logging 9.0 / Sinks.File 6.0 / Sinks.Debug 3.0): daily rolling file sink at `FileSystem.AppDataDirectory/logs/trsetup-.log` (14-file retention) + Debug sink; `ClearProviders()`+`AddSerilog(dispose:true)` so shared-lib `ILogger<T>` flows in; startup line logs app version/build; `AppDomain.UnhandledException` + `TaskScheduler.UnobservedTaskException` → `Log.Fatal`; `Log.CloseAndFlush()` on `Window.Destroying` (App.xaml.cs). Libraries keep MEL abstractions only (no Serilog ref). VERIFIED: booted the MAUI head (rung #4) — log file created at `…\User Name\com.techierathore.trsetup\Data\logs\trsetup-20260709.log` containing the startup line AND a `TrSetup.Core.Settings.JsonSettingsStore` event (proves the shared-library `ILogger<T>`→Serilog pipeline). | [view](#d-req-nfr-007) |
| REQ-UI-006 | Settings screen — endpoints + profile details in the MAUI app (P6) | Verified | 100 | 2026-07-20 RE-CONFIRMED (verify-phase phase-mac, live Mac :5999, render §4a + visual §4b gates PASS at 1280x800 and 390x844, screenshots inspected): settings screen PASS 2/2 — all regions render, endpoint validation gates Save, edit persists. ‖ PRIOR: VERIFIED 2026-07-10 (executed verify-phase, run ledger docs/.last-verify.json): `tests/verify/settings.spec.ts` 2/2 PASS against the live :5999 host — /settings reachable via `NavSettings`, ALL regions render real data (§4a: 4 role cards, app select, `endpoint-input-MacIp` populated `192.168.1.77`, profile-details table with 8 AppStudio rows + built-in Source badges, settings-file path footer), endpoint validation gates Save, edit PERSISTS across reload (`JsonSettingsStore`); §4b visual-truth: desktop screenshot `test-results/settings-desktop.png` INSPECTED — clean layout, no overlap/clip. ⚠ visual (minor, non-blocking, unchanged): 390px sidebar-shell overflow = the documented REQ-UI-001 desktop-tool caveat. HISTORY — DEMOTED 2026-07-10 earlier that day: the "VERIFIED" below was SELF-ATTESTED by the build orchestrator — verify-phase was never executed (agent-confirmed). 2026-07-09 [REQ-UI-006] Built `/settings` (TrSetupUI, from UIDesign §Settings control map): sidebar-reachable (`NavSettings`), header + "Back to board"; role cards + native-dev switch (REQ-UI-003 pattern), app `Select`, per-endpoint `Input` (today `MacIp`) with IP/hostname validation (blank allowed; error + Save-gate while invalid), read-only profile-details `Card`+table with a **Source** column (built-in vs `.tfcore/trsetup-profile.json` override), settings-file-path footer, Save/Cancel. Added `ProfileLoader.ResolveWithSources` + `RequirementSource`/`ResolvedRequirement` (read-only source surfacing; `Resolve` + board merge untouched) and `BoardState.SaveSettingsAsync`/`Endpoints`. Every control has a stable `data-testid`. VERIFIED: Playwright render+visual (desktop 1280×800) — all regions render, endpoint validation gates Save, edit **persists** across reload (`JsonSettingsStore`) and the board re-scopes; the other 5 UI pages still pass (no regression). ⚠ visual (minor, non-blocking): at 390px the sidebar-shell content area exceeds the viewport (right-aligned header button off-screen) — the SAME documented desktop-tool caveat as REQ-UI-001; desktop is the target. | [view](#d-req-ui-006) |
| REQ-FN-035 | Rename `TrSetup.App` → `TrSetup` — the primary head carries the product name (P6) | Verified | 100 | 2026-07-10 [REQ-FN-035] Renamed dir `src/TrSetup.App`→`src/TrSetup` + `TrSetup.App.csproj`→`TrSetup.csproj`; sln entry updated (GUID kept); `RootNamespace`→`TrSetup`, all head namespaces `TrSetup.App`→`TrSetup` (Windows WinUI shim →`TrSetup.WinUI`), XAML `x:Class` + manifest identity + scoped-CSS link `TrSetup.styles.css` updated; bundle id `com.techierathore.trsetup` + AutomationIds unchanged. Docs/harness paths updated (BuildAndRun §2/§3, DevGuide, UsageGuide runbook, BRD, Architecture, samples/README, CLAUDE.md); repo grep = only rename-context/historical mentions remain (+ `x:Class="TrSetup.App"` = the new full type name of class App in ns TrSetup). SMOKED: sln Debug build rung #4 0W/0E (5 proj); guide-§2 Release cmd verbatim → `win-x64/TrSetup.exe` 0W/0E; exe BOOTED on Windows (PID observed, persisted ~90s, clean kill, 0 leftover) and wrote today's Serilog log — startup line "TrSetup starting" + a `TrSetup.Core` `ILogger<T>` event; Core.Tests 126/126. VERIFIED 2026-07-10 (executed verify-phase, run ledger docs/.last-verify.json): acceptance met — `src/TrSetup/TrSetup.csproj` builds green rung #4 (0W/0E) AND the app boots (live PID + Serilog evidence above); repo grep = no `TrSetup.App` refs in source/sln/test-harness (only rename-context + dated historical remarks in docs, and `x:Class="TrSetup.App"` = the new full type name of class `App` in namespace `TrSetup`); post-rename smoke + verifier re-run PASS (7/7 UI specs render+visual against :5999, 126/126 unit). | [view](#d-req-fn-035) |
| REQ-FN-036 | Zero-prerequisite distributable — runs on a fresh machine with no .NET/MAUI (BRD-57, P7) | PARTIAL | 50 | 2026-07-21 [REQ-FN-036/037/039/040/041] DOCS — authored `docs/TrSetup-DeploymentGuide.md` (+ rendered .html): the end-to-end production-deployment path — artifact matrix per platform, the exact build commands and their REQUIRED host OS, self-containment verification, release/download UX, first-run role/profile/AppManagerUrl/APPMANAGER_API_KEY configuration, the Gatekeeper + SmartScreen trust gates with the current signing posture, a deployment-verification recipe, troubleshooting, and an explicit not-yet-proven section. macOS facts RE-CONFIRMED live this session: `codesign -d` on the built bundle = `Signature=adhoc` / `TeamIdentifier=not set`, `spctl -a -t exec` = **rejected**, 56 `System.*` assemblies in Contents/MonoBundle, .app 156 MB / .dmg 49 MB, MacOS binary universal (x86_64 + arm64). The Windows artifact is documented as **UNTESTED END-TO-END** throughout (never built, never launched) — no status/% changed here and NOTHING marked Verified; that remains owner UAT on a clean Windows image. SMOKE (mandatory, this Mac, TrSetup.Web :5999, roles DeviceHostMac+AppRunnerMac+NativeDev, AppStudio; new spec tests/verify/deployment-guide-smoke.spec.ts, PASS): drove the guide's own verification recipe against the running board — 16 rows rendered and ALL settled (none Pending): 11 Pass / 1 Warn / 4 Fail; `appstudio.appmanager-api` green with `Endpoint https://192.168.1.14:5101/health [configured in Settings → Endpoints ['AppManagerUrl'], self-signed certificate explicitly trusted] answered 200.`; `appstudio.appmanager-secret` correctly still red (owner-supplied, ADR-008, not faked); /settings rendered the endpoint override + trust opt-in + settings-file path with 0 horizontal overflow. Screenshots INSPECTED: test-results/deployguide-board-desktop.png, deployguide-settings-desktop.png. ← 2026-07-20 [REQ-FN-036] MAC HALF MET, WINDOWS HALF OPEN. Mac: confirmed self-contained — 56 System.* assemblies in TrSetup.app/Contents/MonoBundle, and the packaged .dmg app launched on this machine with the SDK reachable only via ~/.dotnet (not a system install). Windows: still non-compliant — see REQ-FN-039. Row cannot close until the Windows artifact is proven on a clean Windows image. ←  2026-07-20 [REQ-FN-036] NEW — owner vision: "user downloads the .dmg/.exe from the repo and runs it on a fresh machine and this setup installs everything". Bootstrap must be non-circular: TrSetup installs .NET, so it must never require .NET. **Mac already compliant** (Catalyst .app bundles the runtime — 56 System.* assemblies in Contents/MonoBundle, verified 2026-07-20). **Windows NOT compliant** — src/TrSetup/TrSetup.csproj declares no SelfContained / RuntimeIdentifier / WindowsAppSDKSelfContained, so the unpackaged exe needs the .NET Desktop Runtime + Windows App SDK already present, which is the exact state TrSetup exists to repair. Acceptance: on a clean OS image with no developer tooling, the downloaded artifact launches to the board. | — |
| REQ-FN-037 | Mac `.dmg` packaging (BRD-58, P7) | Implemented | 75 | 2026-07-20 [REQ-FN-037] VERIFIER EVIDENCE (verify-phase phase-mac): the packaged artifact was **mounted and inspected** — `hdiutil attach artifacts/TrSetup-1.0-macOS.dmg` succeeds and the volume carries the correct drag-install layout: TrSetup.app (156 MB) + an /Applications symlink; detached cleanly. Packaging demonstrably works. NOT promoted to Verified: this REQ (and FN-036/038/039/040/041) has **no Details anchor and no acceptance criteria** in this checklist, so there is nothing to grade against — see the verifier miss list. Gatekeeper/first-launch trust is separately FN-038 (owner-parked) / FN-040. ‖ PRIOR: 2026-07-20 [REQ-FN-037] BUILT (flow-master, Mac). `build/package-mac.sh` stages the Catalyst `.app` + an `/Applications` symlink and `hdiutil create -format UDZO`s a versioned image into `artifacts/`. Smoke: produced `artifacts/TrSetup-1.0-macOS.dmg` (49 MB); mounted at /Volumes/TrSetup, drag-to-Applications layout present, `TrSetup.app` inside verified un-sandboxed (`app-sandbox => false`) and self-contained (56 System.* assemblies in Contents/MonoBundle), signature valid, detached cleanly. NOT Verified: no verify-phase run. ←  2026-07-20 [REQ-FN-037] NEW — produce a mountable .dmg (drag-to-Applications layout) as a release asset. Today `CreatePackage=false` in TrSetup.csproj deliberately emits a bare runnable .app and the guide tells the user to copy it and clear quarantine by hand — workable for the owner, impossible for an end user on a fresh Mac. Acceptance: `.dmg` mounts, drag-install works, launched app is the same build. | — |
| REQ-FN-038 | Gatekeeper-clean first launch — Developer ID signing + notarization + stapling (BRD-59, P7) | Blocked | 0 | 2026-07-20 [REQ-FN-038] PARKED BY OWNER — decision 2026-07-20: no Apple Developer account for now; ship unsigned via GitHub Actions instead (REQ-FN-041) and rely on the documented one-time trust step (REQ-FN-040). Remains Blocked, not withdrawn: the defect it describes is real and reproduced (quarantined download => `spctl` **rejected**), it simply cannot be closed without a paid Developer ID certificate. Revisit if TrSetup is distributed beyond the owner's own machines. ←  2026-07-20 [REQ-FN-038] NEW — **BLOCKED on external dependency: paid Apple Developer account + Developer ID certificate (owner decision, ~$99/yr).** Current build is `Signature=adhoc, TeamIdentifier=not set` (verified via codesign 2026-07-20); macOS blocks any quarantined download so signed. Cannot be closed by an agent. Interim mitigation tracked by REQ-FN-040. Acceptance: downloaded .dmg opens on a fresh Mac with no terminal command and no right-click→Open. | — |
| REQ-FN-039 | Windows self-contained artifact (BRD-60, P7) | Implemented | 60 | 2026-07-20 [REQ-FN-039] CSPROJ CHANGES MADE + PROPERTY-VERIFIED, WINDOWS PUBLISH NOT YET RUN (flow-master, Mac). Added a windows-target-scoped PropertyGroup to src/TrSetup/TrSetup.csproj: `SelfContained=true`, `WindowsAppSDKSelfContained=true` (required for an unpackaged app — WindowsPackageType=None has no MSIX framework dependency to resolve WinUI 3 from), `RuntimeIdentifiers=win-x64;win-arm64`, and `RuntimeIdentifier` defaulted to win-x64 ONLY when the caller passes none (so plain `dotnet build` still works and `-r win-arm64` still overrides). VERIFIED on this Mac by MSBuild property evaluation (`-p:EnableWindowsTargeting=true`): for the windows TFM the graph resolves SelfContained=true / WindowsAppSDKSelfContained=true / RuntimeIdentifier=win-x64 / RuntimeIdentifiers=win-x64;win-arm64 / WindowsPackageType=None (preserved); passing `-p:RuntimeIdentifier=win-arm64` correctly yields win-arm64, proving the conditional default does not clobber an explicit RID. NO LEAK into the Mac build: on the maccatalyst TFM `WindowsAppSDKSelfContained` and `RuntimeIdentifiers` both evaluate EMPTY (the `SelfContained=true`/`maccatalyst-arm64` seen there are set by the MAUI SDK itself, not by this block); mac `.app` build + web build + 142/142 unit tests all still green after the change. Also added build/package-windows.ps1 — self-contained publish + a hard self-containment assertion (fails the build unless System.Private.CoreLib.dll, Microsoft.WindowsAppRuntime* and Microsoft.UI.Xaml.dll are all beside the exe) + zip. Deliberately NOT using PublishSingleFile (WinUI 3 / Windows App SDK does not reliably support it — native assets must sit beside the exe; ship the folder in a zip or point Inno Setup/WiX at it) and NOT PublishTrimmed (unsupported for MAUI/WinUI). ⚠ NOT VERIFIED END-TO-END: the actual Windows publish CANNOT be produced on this Mac — attempted twice and both failed. The repo's TargetFrameworks guard deliberately REPLACES the list with net10.0-maccatalyst on macOS (that guard exists to fix NETSDK1100), and forcing it via `-p:TargetFrameworks=` propagates the global property into TrSetupUI/TrSetup.Core and corrupts the graph (NETSDK1005: assets file has no target for net10.0). Needs a Windows host. OWNER UAT: run `.\build\package-windows.ps1` on Windows, then launch the zip's exe on a CLEAN Windows image with no .NET Desktop Runtime and no Windows App SDK — that clean-machine launch is the only thing that actually closes BRD-57/BRD-60. ← 2026-07-20 [REQ-FN-039] NEW — self-contained publish + WindowsAppSDKSelfContained so the exe runs with no .NET Desktop Runtime and no Windows App SDK preinstalled. Reinstates, for the surviving MAUI head only, the self-contained-publish capability withdrawn with BRD-45. Note: this is the likely cause of the owner's original 2026-07-20 report that "the exe build failed to install" on a machine without tooling. Expect a large artifact (self-contained MAUI ≈150 MB+); size is an accepted trade for a non-circular bootstrap. Acceptance: exe launches on a clean Windows image. Not buildable on this Mac — needs a Windows host. | — |
| REQ-FN-040 | Documented unsigned first-run trust path (BRD-61, P7) | Implemented | 75 | 2026-07-20 [REQ-FN-040] WRITTEN + EMPIRICALLY VALIDATED (flow-master, Mac). Added docs/TrSetup-UsageGuide.md §1b covering macOS (right-click→Open, or `xattr -dr com.apple.quarantine`) and Windows SmartScreen (More info→Run anyway), stating plainly that releases are unsigned and why. Validation was not assumed — the fresh-machine download was SIMULATED: set `com.apple.quarantine` on the .dmg as Safari would, mounted it, copied the app out; the flag PROPAGATED to the .app and `spctl -a -t exec` returned **rejected** (proving REQ-FN-038 is a real user-facing blocker, not theoretical); then applying the documented `xattr -dr` cleared it and the app LAUNCHED (pid confirmed). NOT Verified: no verify-phase run. ←  2026-07-20 [REQ-FN-040] NEW — while REQ-FN-038 stays blocked, the release page + UsageGuide must state plainly that the build is unsigned and give the exact one-time open steps (macOS: right-click → Open, or `xattr -dr com.apple.quarantine`; Windows: SmartScreen More info → Run anyway). Shipping a Gatekeeper-blocked artifact with no instructions is the current de-facto behaviour and must not ship. Agent-completable without the Apple account. | — |
| REQ-FN-041 | CI build pipeline — GitHub Actions produces both distributables, no Apple account (BRD-57/58/60, P7) | Implemented | 70 | 2026-07-20 [REQ-FN-041] NEW + BUILT (flow-master, owner request "better build using github actions without account for now"). Added `.github/workflows/build-artifacts.yml`: macos-15 job installs .NET 10 + maui-maccatalyst, runs the unit suite, builds the Catalyst .app and packages it via build/package-mac.sh; windows-latest job matrixes win-x64 + win-arm64 (fail-fast off so an arm64 failure cannot hide a good x64 artifact), runs the unit suite and build/package-windows.ps1. Both upload artifacts; a `v*` tag additionally publishes a GitHub Release whose body carries the unsigned-install trust steps verbatim — a blocked download with no explanation is indistinguishable from a broken one (REQ-FN-040). Signing needs NO secrets: the Catalyst build uses codesign's ad-hoc identity (CodesignKey '-'), so no Apple Developer certificate is involved. Self-containment is ASSERTED in CI, not assumed: the mac job fails the build if fewer than 10 System.* assemblies are bundled in Contents/MonoBundle, and package-windows.ps1 already hard-fails unless System.Private.CoreLib.dll + Microsoft.WindowsAppRuntime* + Microsoft.UI.Xaml.dll all sit beside the exe — BRD-57's non-circular bootstrap cannot silently regress. ⚠ ONE REPOSITORY SECRET REQUIRED BEFORE THE FIRST RUN: `GH_PACKAGES_PAT` (a PAT with read:packages for the techierathore feed). TrSetupUI depends on TrBlazeUI 1.0.7 which is not on nuget.org, so restore fails without it; the default GITHUB_TOKEN cannot read another owner's package feed. Verified locally: YAML structure, no tabs, all three jobs present, single secret reference, `.github` not gitignored. NOT Verified: the workflow has NOT been executed — it cannot run until the secret exists and the branch is pushed. First run is owner UAT. | — |

**Status values:** `Not Started` · `In Progress` · `Implemented` (code done, not yet verified) · `Verified` (self-smoke or verifier PASS — acceptance AND data-render AND visual gates all pass) · `Done (pre-existing)` (migrated as already complete — build agents must NOT rebuild; terminal like `Verified`) · `Needs re-verify` (a defect or change was logged — must be re-run before it can return to `Verified`) · `PARTIAL` (some acceptance unmet — say what in Remarks) · `FAIL` (verifier ran and failed — bug in Remarks) · `Blocked` (external/library gap — link the TR-/TR-RAG- entry in Remarks) · `N/A`.

**% guide:** `0` not started · `25` scaffolded · `50` in progress · `75` implemented-unverified · `100` verified.

**Remarks:** date + what was done / what is missing / bug or library reference. This is the home for bugs and change notes — do not spawn a separate file. Visual-gate failures are prefixed `⚠ visual:`; security findings `⚠ SECURITY`.

## UI / Pages

<!-- Built by /trblazeui from the approved mockups (docs/TrSetup-UIDesign.md + docs/mockups/*.html). -->

### Page: Board dashboard (`/`)

<a id="d-req-ui-001"></a>
- **REQ-UI-001** — Grouped red/amber/green board: header role + app selectors; groups (Framework core / Bridges / <App> profile / Build & run) with ✓/⚠/✗ counts and per-group Fix all; failing rows show Preview + Fix (or Open guide when manual-only); Re-check all + Export report actions; single-check re-check on each row (BRD-9, BRD-10, BRD-12, BRD-13, Phase 1). *Mockup:* docs/mockups/dashboard.html.
  - *Acceptance:* board renders grouped checks with live status icons + text; role/app switch re-scopes rows without reload; Fix hidden and "Open guide" shown for manual-only checks; controls do not overlap at desktop + mobile widths (visual gate).

### Page: Check detail sheet (`/check/{id}`, sheet over `/`)

<a id="d-req-ui-002"></a>
- **REQ-UI-002** — Detail pane per check: Explain (what/why + WORKFLOW §/guide link), Detect evidence, Fix preview (literal commands/URLs), last run output (command, stdout/stderr, exit code) in an expandable pane (BRD-11, Phase 1). *Mockup:* docs/mockups/check-detail.html.
  - *Acceptance:* row click opens the sheet with all four sections populated; output block scrolls inside the pane; deep link `/check/{id}` lands on the same content.

### Page: First-run role picker (`/setup`)

<a id="d-req-ui-003"></a>
- **REQ-UI-003** — First-run role picker: one card per machine role with a one-line explanation; multi-select; "I develop natively on this machine" variant; default-app selector; saves and proceeds to the board (BRD-6, BRD-8, Phase 1). *Mockup:* docs/mockups/role-picker.html.
  - *Acceptance:* shown automatically when no settings file exists; selections persist (REQ-FN-005) and the board scopes to them; reachable later from the board header.

### Page: Fix-all run view (`/fix-run`)

<a id="d-req-ui-004"></a>
- **REQ-UI-004** — Sequential fix-run progress: ordered step list with per-step status, live output stream, explicit consent gate (shows FixPreview, Approve/Decline) before each elevated step, halt-on-decline; summary with failed steps + raw output (BRD-29 UI, Phase 2). *Mockup:* docs/mockups/fix-run.html.
  - *Acceptance:* steps execute in dependency order; a declined consent halts the run leaving later steps untouched; failed step shows raw output inline.

### Page: Settings / Configuration (`/settings`)

<a id="d-req-ui-006"></a>
- **REQ-UI-006** — Settings/Configuration screen, reachable from the board header beside the role/app selectors: edit machine roles + the native-dev variant (reuse the REQ-UI-003 role-card patterns), change the selected app profile, **edit named endpoint values** (`TrSetupSettings.Endpoints` — today `MacIp`; one labelled input per known endpoint, validated as an address, never a secret per ADR-008), show the settings-file path (parity with the withdrawn `trsetup config`), and a read-only **profile details** pane for the selected app: the requirement rows the profile contributes, whether they come from the built-in profile or a `.tfcore/trsetup-profile.json` app-repo override, and which one wins (REQ-FN-021 merge rules). Saves via `JsonSettingsStore` (REQ-FN-005); the board re-scopes on save without reload. Replaces the withdrawn CLI configuration surface (REQ-FN-013 picker + `--roles/--app/--mac-ip` flags + `trsetup config`). No separate mockup — extend the role-picker mockup patterns (`docs/mockups/role-picker.html`) per the UIDesign §Settings control map (BRD-56, Phase 6).
  - *Acceptance:* editing the Mac IP persists to the settings JSON and the Bridges checks probe the new address on the next sweep; role/app changes re-scope the board without reload; the profile pane lists the selected app's requirement rows + their source (built-in vs override); every control carries a stable `data-testid`/`AutomationId`.

### Page: Report preview (`/report`)

<a id="d-req-ui-005"></a>
- **REQ-UI-005** — Report preview + export: rendered board report with host/roles/app header; Save as MD + HTML; copy-to-clipboard (BRD-24 UI, Phase 1). *Mockup:* docs/mockups/report.html.
  - *Acceptance:* preview matches the board state at export time; no secret values anywhere in the output (spot-check `env-secret` rows show presence status only).

## Functional requirements

<a id="d-req-fn-001"></a>
- **REQ-FN-001** — `Check` contract in `TrSetup.Core`: `Id, Title, Category, Roles, Severity(Required|Recommended|Optional), DetectAsync→Pass|Warn|Fail|NotApplicable + evidence, Explain, FixAsync?, FixPreview, VerifyAsync` (BRD-1, BRD-2, Phase 1).
  - *Acceptance:* unit tests construct a fake check and drive the full contract; NotApplicable checks never render as failures.

<a id="d-req-fn-002"></a>
- **REQ-FN-002** — Detect → Preview → Fix → Re-verify pipeline: fixes only run after preview+consent; `VerifyAsync` re-detects; a fix that doesn't re-detect green is reported failed with raw output attached — never "assume fixed" (BRD-3, Phase 1).
  - *Acceptance:* unit test with a fixer whose verify fails yields status FAILED + captured output.

<a id="d-req-fn-003"></a>
- **REQ-FN-003** — Process runner: single choke-point for command execution capturing exact command line, stdout/stderr, exit code; async with live output streaming (BRD-4, Phase 1).
  - *Acceptance:* every detect/fix result exposes its evidence trail; long-running command streams output incrementally.

<a id="d-req-fn-004"></a>
- **REQ-FN-004** — Catalog scoping: engine enumerates checks for (machine roles ∩ selected app); supports the four roles + native-dev variant; out-of-scope checks are NotApplicable (BRD-5, BRD-7, Phase 1).
  - *Acceptance:* switching role/app changes the enumerated set per the catalog tables in BRD §9.

<a id="d-req-fn-005"></a>
- **REQ-FN-005** — Settings persistence: roles, selected app, configured endpoints (Mac IP) in a small local JSON file; no DB (BRD-6, Phase 1; Architecture ADR-002).
  - *Acceptance:* restart restores selections; missing file triggers first-run role picker.

<a id="d-req-fn-006"></a>
- **REQ-FN-006** — WSL agent-host detects: .NET SDK, headless-Chromium apt libs, `~/bin/winrun` + PATH, Node, Playwright CLI + Chromium, mirrored-networking probe, Windows Appium `/status`, Mac Appium `/status`, git — per the BRD §9 F-WSLCHK table (BRD-17, Phase 1).
  - *Acceptance:* on this WSL distro each check returns a real status + evidence (run it yourself — the harness is set up).

<a id="d-req-fn-007"></a>
- **REQ-FN-007** — Windows device-host detects: `.wslconfig` mirrored, Android SDK tools, API-34 image, `Pixel_API_34` AVD, Node/npm, Appium + uiautomator2, `start-android-verify.ps1`, Appium `:4723` session, MAUI workload, JDK — per BRD §9 F-WINCHK (BRD-19, Phase 1).
  - *Acceptance:* detects run via the winrun bridge from WSL dev environment or natively on Windows; each yields evidence.

<a id="d-req-fn-008"></a>
- **REQ-FN-008** — Mac device-host detects: Xcode/CLT, .NET SDK + MAUI workload, Node, Appium + xcuitest + mac2, Appium on `0.0.0.0:4723` (LaunchAgent), stable IP vs configured endpoint, iOS Simulator runtime — per BRD §9 F-MACCHK (BRD-21, Phase 1).
  - *Acceptance:* detect logic unit-tested with faked probe outputs; live run deferred to a Mac session.

<a id="d-req-fn-009"></a>
- **REQ-FN-009** — Cross-machine probes are HTTP/ping only; never remote-execute; failing probe guidance names the owning machine role (BRD-23, Phase 1).
  - *Acceptance:* unreachable endpoint yields Fail + "run TrSetup on <machine>" guidance; 5 s timeout; probes run in parallel.

<a id="d-req-fn-010"></a>
- **REQ-FN-010** — Report exporter: full board (groups, statuses, evidence, last-run outputs) → `TrSetup-Report-<host>.md` + `.html` via the shared doc shell; secret values never included (BRD-24, BRD-25, Phase 1).
  - *Acceptance:* exported MD+HTML reproduce the board; `env-secret` rows show presence status only.

<a id="d-req-fn-011"></a>
- **REQ-FN-011** — **[WITHDRAWN 2026-07-09 — owner decision: single MAUI desktop app; see REQ-FN-034]** `TrSetup.Web` Blazor Server head: `trsetup gui` starts Kestrel on `localhost:5999` (fallback to a free port) and opens the Windows browser via mirrored networking (BRD-14, Phase 1).
  - *Acceptance:* Playwright drives the board at `http://localhost:5999` headlessly in WSL.

<a id="d-req-fn-012"></a>
- **REQ-FN-012** — **[WITHDRAWN 2026-07-09 — owner decision: single MAUI desktop app; see REQ-FN-034]** `TrSetup.Cli` Spectre.Console TUI: `trsetup` with no args renders the same grouped board (statuses + evidence) in the terminal, over SSH too; thin renderer over `TrSetup.Core`, never a second implementation (BRD-15, Phase 1).
  - *Acceptance:* TUI shows the identical check set + statuses as the Web head on the same machine.

<a id="d-req-fn-013"></a>
- **REQ-FN-013** — **[WITHDRAWN 2026-07-09 — owner decision: single MAUI desktop app; see REQ-FN-034]** TUI interactions: arrow-key selection, `f` fix, `a` fix-all, `r` re-check, `e` export report (BRD-16, Phase 1; fix keys activate with P2 fixers).
  - *Acceptance:* keys act on the selected row/board; `f` on a manual-only check shows guidance instead.

<a id="d-req-fn-014"></a>
- **REQ-FN-014** — WSL auto-fixers for every ✔ row of F-WSLCHK: dotnet-install SDK, apt libs, winrun write + PATH line, Node LTS, Playwright install, git (BRD-18, Phase 2).
  - *Acceptance:* on a deliberately-broken item, Fix → re-detect green; running twice is a no-op.

<a id="d-req-fn-015"></a>
- **REQ-FN-015** — Windows auto-fixers for every ✔ row of F-WINCHK: `.wslconfig` patch + `wsl --shutdown` prompt, cmdline-tools + SDK + system image + AVD, Node, Appium + uiautomator2, ps1 helper from embedded template, session-test run, MAUI workload, Temurin JDK (BRD-20, Phase 2).
  - *Acceptance:* each fixer previews its literal commands; failed installs surface raw output.

<a id="d-req-fn-016"></a>
- **REQ-FN-016** — Mac auto-fixers for every ✔ row of F-MACCHK: CLT install, SDK + MAUI workload, Node, Appium drivers, LaunchAgent plist write + load, `-downloadPlatform iOS`; full Xcode + DHCP reservation stay manual guidance (BRD-22, Phase 2).
  - *Acceptance:* LaunchAgent survives reboot (documented check); manual-only rows never grow a Fix button.

<a id="d-req-fn-017"></a>
- **REQ-FN-017** — Installer download framework: official sources only, URLs pinned in profile/engine and shown in FixPreview, checksum-verified where published ("no published checksum" recorded otherwise), installs into TrSetup-managed locations that never collide with system installs (BRD-26, BRD-27, Phase 2).
  - *Acceptance:* a tampered download fails the checksum and reports; install paths are under the TrSetup-managed root.

<a id="d-req-fn-018"></a>
- **REQ-FN-018** — Idempotent config writes: everything TrSetup writes (`.wslconfig` block, `.bashrc` PATH line, plists, ps1) carries managed marker blocks; re-runs never duplicate; user edits outside markers never clobbered (BRD-28, Phase 2).
  - *Acceptance:* run fix twice → single block; hand-edit outside block survives a re-fix.

<a id="d-req-fn-019"></a>
- **REQ-FN-019** — Fix-all runner: topological dependency order (Node before Appium, SDK before AVD, Postgres before PgVector…), stops at first consent/elevation gate or declined consent; per-step re-verify; continue-or-stop on failure (BRD-29, Phase 2).
  - *Acceptance:* order verified by unit test on the dependency graph; declined consent halts with later steps untouched.

<a id="d-req-fn-020"></a>
- **REQ-FN-020** — Elevation/consent runner: elevated steps show the exact command and run only on click; Windows UAC in a visible child process; WSL sudo optional interactive-terminal handoff (app prints the one command to paste); no stored passwords (BRD-30, Phase 2).
  - *Acceptance:* no code path stores or caches credentials; every elevated run is user-initiated.

<a id="d-req-fn-021"></a>
- **REQ-FN-021** — Profile schema + loader: `trsetup-profile.json` requirement instances of types `sdk, workload, cli-tool, service, endpoint, nuget-feed, env-secret, disk-space, appium-head, runtime-install`, each role-tagged; built-in + app-repo `.tfcore/trsetup-profile.json` override merge — app repo wins (BRD-33, BRD-34, BRD-35, Phase 3).
  - *Acceptance:* schema-validated load; conflict test proves app-repo wins; new app onboards with a JSON file only.

<a id="d-req-fn-022"></a>
- **REQ-FN-022** — AppStudio built-in profile per BRD §9 F-PROFILES table: .NET 10 SDK, MAUI workload, dotnet+git on PATH, Xcode (Mac-runner), techierathore GitHub Packages feed (PAT `read:packages` authenticates), App Manager API endpoint, AppManager key presence (BRD-36, Phase 3).
  - *Acceptance:* selecting AppStudio renders exactly these rows scoped to the machine's roles.

<a id="d-req-fn-023"></a>
- **REQ-FN-023** — TrStudio built-in profile per BRD §9 F-PROFILES table: .NET 10 SDK, Postgres + PgVector service, ffmpeg, isolated Python + ComfyUI `runtime-install`, model disk-space floor (Mac-runner, warn), TechieRag NuGet PAT (Win), RunPod/HeyGen/AppManager key presence, AppManager endpoint (BRD-37, Phase 3).
  - *Acceptance:* selecting TrStudio renders exactly these rows scoped to roles.

<a id="d-req-fn-024"></a>
- **REQ-FN-024** — Framework-profile extras inside an app repo: offer to write `core-config.yaml → runtimeVerification.appium` from just-verified endpoints, then curl-verify each registered head (BRD-38, Phase 3).
  - *Acceptance:* written block matches the WORKFLOW §0b step-4 shape; existing user config respected (idempotent markers).

<a id="d-req-fn-025"></a>
- **REQ-FN-025** — Runtime-install fixer: isolated Python env + ComfyUI from GitHub releases into a TrSetup-managed location (TrStudio isolated-Python discipline generalized); boundary — TrStudioAdmin owns models/workflows/providers above the runtime (BRD-39, Phase 3).
  - *Acceptance:* ComfyUI starts from the managed install; no collision with any system Python.

<a id="d-req-fn-026"></a>
- **REQ-FN-026** — Service fixers: PostgreSQL install (winget/brew) + `CREATE EXTENSION vector`; ffmpeg install (BRD-40, Phase 3).
  - *Acceptance:* fresh machine reaches "service running + extension present"; re-run is a no-op.

<a id="d-req-fn-027"></a>
- **REQ-FN-027** — Mac app-runner role: aggregates the selected app's profile requirements for local build+run (SDK/workloads, Xcode, git, NuGet auth, services, runtimes, keys) into the board with fixes (BRD-41, Phase 3).
  - *Acceptance:* the role's board equals the app's BuildAndRun-Guide prerequisites section.

<a id="d-req-fn-028"></a>
- **REQ-FN-028** — "Build & install <App> for Mac (Catalyst)" fixer: enabled when prerequisites green; runs `dotnet build -f net10.0-maccatalyst -c Release` and opens/installs the produced `.app` (BRD-42, Phase 3).
  - *Acceptance:* fixer disabled with reason while any prerequisite red; success verified by the `.app` existing.

<a id="d-req-fn-029"></a>
- **REQ-FN-029** — Disk-space check type: configurable floor per profile, warn severity (BRD-43, Phase 3).
  - *Acceptance:* floor breach yields Warn (never Fail) with free/required figures in evidence.

<a id="d-req-fn-030"></a>
- **REQ-FN-030** — `TrSetup` MAUI Blazor Hybrid head (named `TrSetup.App` until the REQ-FN-035 rename): Windows unpackaged exe (`WindowsPackageType=None`) + Mac Catalyst via MAUI, ad-hoc signed; hosts the same `TrSetupUI` RCL (BRD-44, Phase 4).
  - *Acceptance:* Windows head builds via ladder rung #4 and boots to the board; Catalyst build documented for the Mac; AutomationIds present (REQ-NFR-005).

<a id="d-req-fn-031"></a>
- **REQ-FN-031** — Distribution: firm up `docs/TrSetup-BuildAndRun-Guide.md` with concrete paths/ports. **[RESCOPED 2026-07-09 — MAUI-only: the guide covers the `TrSetup` (ex-`TrSetup.App`) Windows exe + Mac Catalyst build; the Cli/Web self-contained publish scripts are withdrawn (removal in REQ-FN-034).]** (BRD-45, Phase 4).
  - *Acceptance:* the rewritten guide's build commands executed verbatim produce a runnable `TrSetup` head.

<a id="d-req-fn-032"></a>
- **REQ-FN-032** — **[WITHDRAWN 2026-07-09 — owner decision: single MAUI desktop app; agent mode lived in the CLI head; see REQ-FN-034]** `trsetup --check --json`: machine-readable board (checks, statuses, evidence) with stable schema for agents/CI; exit code reflects overall status (BRD-46, Phase 5).
  - *Acceptance:* JSON schema documented; verifier can parse it; no UI required.

<a id="d-req-fn-033"></a>
- **REQ-FN-033** — **[WITHDRAWN 2026-07-09 — consumed the withdrawn FN-032 agent mode; see REQ-FN-034]** (Stretch) verify-phase §0 pre-flight hook: the TechieFlow verifier runs `trsetup --check --json` and gates on "environment green" (BRD-47, Phase 5).
  - *Acceptance:* documented hook + example gate; explicitly optional.

<a id="d-req-fn-034"></a>
- **REQ-FN-034** — Decommission the CLI and Web heads (owner decision 2026-07-09: TrSetup ships as ONE MAUI Blazor Hybrid desktop app). Remove `src/TrSetup.Cli`, `src/TrSetup.Web`, `tests/unit/TrSetup.Cli.Tests` from disk and `TrSetup.sln`; remove `scripts/publish.sh`, `scripts/publish.ps1`, `scripts/preflight-gate.sh`, `scripts/preflight-gate.ps1`; keep the MAUI head (named `TrSetup.App` at the time; `TrSetup` since the REQ-FN-035 rename), `TrSetupUI` (RCL), `TrSetup.Core` (engine), `tests/unit/TrSetup.Core.Tests`. Also migrate the verification harness: `playwright.config.ts` currently boots `TrSetup.Web` on :5999 as its test host — rehost the UI smoke path against the MAUI head via the Windows-MAUI/Appium bridge (or retire the Playwright config with the Web head and note the replacement in the UsageGuide runbook). Docs were rewritten MAUI-only on the decision date; this REQ is the code-side cleanup (Phase 6).
  - *Acceptance:* the solution contains exactly the four kept projects and builds green via ladder rung #4 (0W/0E); `TrSetup.Core.Tests` pass; no source or test-harness references to `TrSetup.Cli`/`TrSetup.Web` remain outside `docs/OldDocs/`.

<a id="d-req-fn-035"></a>
- **REQ-FN-035** — Rename the MAUI head `src/TrSetup.App` → `src/TrSetup` per the framework naming standard (owner rule 2026-07-10: the product's primary executable head is named exactly `<APP>`; the `.App` suffix is banned — coding-standards §"Project & solution naming"). Scope: rename the project directory + `TrSetup.App.csproj` → `TrSetup.csproj`; update the `TrSetup.sln` entry; update `RootNamespace`/`AssemblyName` and the `TrSetup.App.*` namespaces + their `using`s across the head; update any harness/scripts/docs paths that reference `src/TrSetup.App` or `TrSetup.App.exe` (BuildAndRun guide §2/§3, DevGuide, UsageGuide runbook). The Mac bundle id `com.techierathore.trsetup` and all `AutomationId`s stay unchanged (Appium bindings unaffected) (Phase 6).
  - *Acceptance:* `src/TrSetup/TrSetup.csproj` builds green via ladder rung #4 (0W/0E) and the app boots; no `TrSetup.App` references remain in source, sln, test harness, or docs outside `docs/OldDocs/`; smoke + verifier re-run pass.

## RAG / AI requirements (→ /techierag)

*None — TrSetup has no AI/RAG features (Architecture ADR-003). The report export exists precisely so a human can hand the board to a Claude session.*

## Non-functional

<a id="d-req-nfr-001"></a>
- **REQ-NFR-001** — Performance per BRD §11 table: full detect sweep < 30 s typical (parallel probes, 5 s timeouts), single re-check < 5 s, first paint < 2 s with streaming detects, UI never blocked by fixes (BRD-48, Phase 1 ongoing).

<a id="d-req-nfr-002"></a>
- **REQ-NFR-002** — Security stance: consent-per-elevation, pinned official sources + checksums where published, presence-only secrets (never stored/displayed/logged/exported; paid APIs never probed), outbound-only networking, no telemetry (BRD-31, BRD-32, BRD-49, Phase 2 — stance applies from P1).

<a id="d-req-nfr-003"></a>
- **REQ-NFR-003** — Accessibility: GUI board fully keyboard-navigable with visible focus; status = icon + text, never color alone; TrBlazeUI aria patterns respected (BRD-50, Phase 1).

<a id="d-req-nfr-004"></a>
- **REQ-NFR-004** — Reliability: an interrupted/failed fix leaves the machine no worse (idempotent fixes, TrSetup-managed locations); re-running always safe (BRD-51, Phase 2).

<a id="d-req-nfr-005"></a>
- **REQ-NFR-005** — Testability: stable `data-testid` on every interactive Blazor control; `AutomationId` on every MAUI control; `--check --json` as CI assertion surface (BRD-52, Phase 1 ongoing).

<a id="d-req-nfr-006"></a>
- **REQ-NFR-006** — **[WITHDRAWN 2026-07-09 — owner decision: single MAUI desktop app; the Cli/Web bootstrap path retires with its heads; see REQ-FN-034]** Portability: Cli and Web heads run from self-contained publishes on machines with no .NET installed — the bare-Mac/WSL bootstrap path (BRD-53, Phase 4).

<a id="d-req-nfr-007"></a>
- **REQ-NFR-007** — Observability: Serilog file-based logging in the MAUI head per the TechieFlow standing NFR (coding-standards §Logging) — wire Serilog in `MauiProgram.CreateMauiApp` with a rolling file sink under `FileSystem.AppDataDirectory/logs/trsetup-.log` (daily rolling, ~14 files retained) + console; `builder.Logging.AddSerilog()`; log unhandled exceptions (`AppDomain.CurrentDomain.UnhandledException`, `TaskScheduler.UnobservedTaskException`) and `Log.CloseAndFlush()` on exit; `TrSetupUI`/`TrSetup.Core` keep logging through `ILogger<T>`/abstractions only (no Serilog reference in libraries) — the head's sink picks their events up automatically (Phase 6).
  - *Acceptance:* launching the app creates the log file and writes startup + check-sweep events through `ILogger<T>` calls from Core; a thrown unhandled exception lands in the file; libraries have no Serilog package reference.
